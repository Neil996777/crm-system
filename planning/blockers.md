# Blockers — CRM System

This register tracks open blockers and pre-gate decisions surfaced during planning.
It is a companion to `planning/gate-status.md` (the gate sync point). On a kickback,
set the affected gate to `Gate Blocked` in gate-status, record it here, and return
to Claude. See `../../../company/collaboration-model.md`.

Status values: `Open` / `In Review` / `Resolved` / `Formal Scope Change by User`.
No-downgrade rule applies: a blocker touching a P0/P1 acceptance may only move to
`Resolved` (with the deciding source recorded) or `Formal Scope Change by User`.

## UI/UX Post-G12 Spot-Fix — RESOLVED (release-owner live-use feedback, 2026-06-08→09)

Found by the release owner on the live dashboard immediately after UI/UX G12 PASS.
Both are genuine; the A6 clipping is a real miss in Claude's closing sweep (fidelity
checked structure + Card→Focus, but not per-card content fit under the fixed row
height). Frontend-only fix; both **Resolved** after Claude re-verification 2026-06-09
(independent build + e2e 49/49 + 1440px dashboard screenshot). See gate-status
Post-G12 spot-fix row = Gate Passed.

| ID | Finding | Owner | Blocks | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-UIUX-G12-004 | **Dashboard card content clipped (A6 no-overflow).** `frontend/src/styles/design-system.css` `.managerDashboardGrid { grid-auto-rows: 268px }` (and `.salesDashboardGrid { 308px }`) force a fixed row height while `.dashboardPanel { overflow: hidden }` crops anything taller — so 团队销售漏斗 (header + 5 funnel rows) and 团队商机阶段构成 (donut + 6-item legend) lose their last rows. Fix frontend-only: let cards size to content (e.g. `grid-auto-rows: minmax(268px, auto)` / `auto`, and/or drop the cropping `overflow:hidden`) so funnel and donut legend fully show without clipping; verify at 1440px and wider; keep visual rhythm. | Codex (execution) | UI/UX G12 re-confirm | A6, C6 | **Resolved** | 2026-06-08 |
| | **Codex return (2026-06-09):** `.managerDashboardGrid` / `.salesDashboardGrid` now use content-adaptive `grid-auto-rows: minmax(..., auto)`; `.dashboardPanel`, `.pipelineViz`, `.dashboardList`, `.paymentRows`, and `.timeline` no longer crop dashboard content. `overview.spec.ts` adds 1440px and 1680px per-card scroll/client checks, plus the existing 900px no-horizontal-overflow check. Evidence: `delivery/uiux-g12-post-pass-spotfix-evidence-2026-06-09.md`. Pending Claude re-verification; Codex does not self-resolve. | | | | | |
| | **Claude re-verification (2026-06-09): RESOLVED.** Independent re-audit: `npm run build` PASS (`index-rHR7gSrr.css`/`index-CsDtRBPy.js` — same hash as Codex → same source); full `npm run test:e2e` PASS 49/49, 0 skip. `TEST-UIUX-A6-001` now programmatically measures every card's `scrollHeight-clientHeight`/`scrollWidth-clientWidth` at 1440px and 1680px = 0 overflow (objective no-clip proof). Live 1440px manager screenshot confirms all 8 cards render full content — 团队销售漏斗 5 rows and 团队商机阶段构成 donut+legend no longer cut at card bottom. 0 backend/`shared`/root-`api` diff; CSS focus styling uses existing `--primary` (no new color). | | | | | |
| BLK-UIUX-G12-005 | **Card→Focus trigger should be the whole card, not a corner button.** `frontend/src/pages/WorkOverview.tsx` `DashboardPanel` triggers focus only via the corner `button.expand` (`onClick={onFocus}`). Release-owner intent: clicking anywhere on the card block enters Card→Focus. Fix frontend-only: make the whole `.dashboardPanel` activate `onFocus` (clickable + keyboard-accessible — role/tabIndex + Enter/Space, or wrap as a button) while keeping the expand icon as a visual affordance; avoid nested-interactive a11y issues; terminal/read-only semantics unaffected. | Codex (execution) | UI/UX G12 re-confirm | A2, A5 (UX intent) | **Resolved** | 2026-06-08 |
| | **Codex return (2026-06-09):** `DashboardPanel` itself is now the Card→Focus trigger (`role="button"`, `tabIndex=0`, click, Enter, Space); the expand glyph remains visual-only (`aria-hidden`) so there is no nested interactive control. `overview.spec.ts` now clicks whole cards for manager/sales focus and asserts keyboard Space activation. Evidence: `delivery/uiux-g12-post-pass-spotfix-evidence-2026-06-09.md`. Pending Claude re-verification; Codex does not self-resolve. | | | | | |
| | **Claude re-verification (2026-06-09): RESOLVED.** Confirmed in source: `DashboardPanel` `<article>` carries `role="button"`+`tabIndex=0`+`onClick`+`onKeyDown` (Enter/Space), expand glyph downgraded to `<span aria-hidden>`; card children are pure data viz (FunnelOverview/StageOverview/…) with no nested button/link → no nested-interactive a11y issue. Live screenshot: clicking the funnel card **body** (not a corner button) enters Card→Focus — funnel expands to detail + `DataTable`, other 7 cards collapse to right `sideCards` strip (design-system §10). `TEST-UIUX-DASHBOARD-001/002` assert whole-card `role=button`+`tabindex`+click; `TEST-UIUX-A7-001` asserts keyboard Space activation — assertions strengthened, none weakened. | | | | | |

## UI/UX Post-G12 Spot-Fix #2 — A7 Card→Focus motion — RESOLVED (release-owner live-use feedback, 2026-06-10)

Release owner observed in live Card→Focus that switching/entering/exiting has **no
transition** — the stage block just blinks in/out ("一会出现一会消失"). Root cause:
the locked **B2 hero transition** (`docs/ux-ui/interaction-spec.md` Part B) was never
implemented — `WorkOverview.tsx` conditionally mounts two different DOM subtrees
(grid vs `FocusStage`) and React swaps them instantly; there is no enter/exit/switch
motion layer and no `.stage`/`.shell.focusMode` transition. Same "end-states built,
motion not realized" family as BLK-UIUX-G12-003/004. Release owner elected
**full B2 implementation** (2026-06-10) — no scope reduction. Frontend-only.

| ID | Finding | Owner | Blocks | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-UIUX-G12-006 | **Card→Focus hero transition (B2) not realized — A7 motion gap.** Locked spec `interaction-spec.md` Part B requires: **enter** ≤`motion-slow` 320ms `ease-standard`, 5-step choreography (0–80ms scrim fade-in + clicked card lift; 40–320ms clicked card translates/scales into the stage slot with inner content compact→full crossfade; 80–300ms other panels stagger-collapse 24ms-step into the right `92px` strip rail; 80–300ms sidebar 248→72px as a transform with labels fading `motion-fast`; settle: focus-visible ring moves to stage heading + live-region announce); **reverse (返回/Esc)** symmetric on `motion-base` 220ms, scrim out `ease-accelerate`, focus restored to the opened grid panel; **switching focus between rail strip cards** = quick crossfade on `motion-base` (NOT full reverse+forward); transform/opacity only (no layout-triggering width/height animation), target 60fps. Current impl: none — instant subtree swap. Ancillary gaps to fix in the same pass: (a) **B1 motion-token scale mismatch** — `frontend/src/styles/motion.css` defines `--motion-fast:120ms/--motion-medium:180ms/--motion-slow:240ms` but locked B1 is `motion-instant 80 / motion-fast 140 / motion-base 220 / motion-slow 320`; UI must mirror B1 names+values exactly (add `--motion-instant`, `--motion-base`; correct durations) without breaking existing token consumers. (b) **false-assurance e2e** — `TEST-UIUX-A7-001` only asserts "reduced-motion snaps", which passes trivially while everything snaps; it must instead assert the transition actually runs in normal-motion (e.g. stage element animates / is present during a measurable window) AND that reduced-motion is a *separate* real path (B6: state still changes, no travel, no info lost). | Codex (execution) | UI/UX G12 A7 re-confirm | A7 (P-level acceptance), C6 (motion tokens), B1/B2/B6 | **Resolved** | 2026-06-10 |
| | **Codex return (2026-06-10):** B1 token scale aligned to locked 80/140/220/320ms values (`--motion-medium` retained as `--motion-base` alias); Card→Focus B2 motion implemented with measured source/target rect FLIP, `motionPhase` enter/exit/switch states, named dashboard keyframes, staggered strip-card entry/exit, focus restoration/live-region handoff, and a separate reduced-motion opacity-only path (`data-motion-mode="reduced"`, `transform:none`). `TEST-UIUX-A7-001` now records `animationstart` events and asserts full-motion enter/strip/switch/exit plus reduced-motion non-travel behavior. Evidence: `delivery/uiux-g12-a7-motion-evidence-2026-06-10.md`. Pending Claude re-verification; Codex does not self-resolve. | | | | | |
| | **Claude re-verification (2026-06-10): RESOLVED — full B2 realized.** Independent re-audit (path B, after Codex docs-only return): rebuild produced byte-identical `index-CgVjA1iB.css`/`index-Ctu9eh-s.js` (same as pre-doc audit → docs-only, code unchanged); full `npm run test:e2e` PASS 49/49, 0 skip, no skip/only. CSS choreography audited line-by-line against locked B2 (scrim instant in / base-accelerate out; stage FLIP via measured `--hero-start-*` rect, calc(slow−40ms)+40ms; content compact→full crossfade; strip stagger via `--strip-*-delay`; switch = crossfade only; reduced = `transform:none` + opacity-only). **Live browser probe** (1440px, animationstart recorder): ENTER fired ScrimIn+StageContentIn+StageEnter+7×StripEnter; SWITCH fired only StageSwitch (no Exit → crossfade not leave+re-enter); REVERSE fired ScrimOut+StageExit+StageContentOut+7×StripExit; REDUCED → `data-motion-mode=reduced`, only `dashboardReducedFocusAppear`, stage `transform:none`, no travel anims. Mid-flight 40ms screenshot shows scrim dimmed + rail mid-collapse + card morphing into stage (not a snap). B1 token scale now mirrors locked 80/140/220/320. 0 backend/`shared`/root-`api` diff; only new color literal is the locked B2 scrim `rgba(15,23,42,.06)` (per `design-system.md` §- no new color); zh-CN intact; enum/role values unchanged; e2e strengthened not weakened. | | | | | |

## UI/UX Post-G12 Spot-Fix #3 — Focus rail = selector model — RESOLVED (release-owner design revision, 2026-06-10)

Release owner accepts the B2 hero **enter** transition but finds the **in-focus switch**
between rail strip cards too jarring/fatiguing. Root cause beyond "no motion":
`WorkOverview.tsx:332` builds the rail from the **7 non-active** cards
(`card.key !== active.key`), so on each switch the rail re-composes — the
previously-focused card pops back into the rail and the clicked one leaves it — while
the switch itself is only a faint content crossfade (`dashboardStageSwitch` scale
.992→1). This matches locked B2 ("other panels collapse to rail" + "switch = quick
crossfade") — so it is spec-faithful, but the release owner has **directed a design
revision** (2026-06-10): **Option A — persistent 8-block selector rail.** This changes
the locked interaction-spec B2 in-focus model and requires a UX decision record.

| ID | Finding | Owner | Blocks | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-UIUX-G12-007 | **Focus rail should be a persistent 8-item selector, not a re-composing 7-card strip (release-owner design revision of locked B2).** New acceptance: (1) In focus mode the right rail lists **all 8 dashboard cards** persistently (including the currently-focused one), order stable; the rail does NOT add/remove items when focus changes. (2) The focused card has a clear **selected state** — accent treatment + `aria-current="true"` (accent uses an existing locked token, e.g. `--primary`; **no new color**). (3) Clicking/Enter/Space on any rail item switches the **left stage** to that panel's detail via the existing calm content crossfade; the rail items themselves do not pop in/out or reorder. (4) Keep the approved **hero enter/exit** transition (BLK-UIUX-G12-006) unchanged — only the in-focus rail composition + switch model changes; on enter the rail settles to all 8 with the opened one selected. (5) Keyboard reachable + reduced-motion path preserved (B6). This is a **design revision to the locked `interaction-spec.md` Part B (B2)** + any `design-system.md` reference to the "other panels collapse" rail — Codex (UX) must update the locked spec with a **decision record (DEC-UX-…)** citing this release-owner direction, then implement. Frontend-only; 0 backend/`shared` diff; zh-CN; enum/role values unchanged; e2e strengthened to assert 8 persistent rail items + selected state + that switching does not change the rail item count/order; build + full e2e green, 0 skip. | Codex (UX design + execution) | UI/UX G12 focus-rail re-confirm | A7 (UX), B2 (locked spec revision), C6 (tokens) | **Resolved** | 2026-06-10 |
| | **Claude re-verification (2026-06-10): RESOLVED — Option A realized.** Independent re-audit: build PASS (`index-CrP9n7t6.css`/`index-BD4d9BTF.js`); full `npm run test:e2e` PASS 49/49, 0 skip, no skip/only. Spec/DEC consistency checked — interaction-spec B2 + design-system both revised to the selector model under DEC-UX-FOCUSRAIL-01 citing this release-owner direction; `WorkOverview` drops the `card.key !== active.key` filter; `.sideCard.selected` accent = `--primary` border + inset bar + existing `--badge-shadow` (no new color). **Live browser probe** (1440px): on enter rail = **8** items incl active; clicking another rail item → rail still **8**, **order unchanged** (`["funnel","stage","trend","leaderboard","todo","payments","key-opportunities","activity"]` identical before/after), exactly **1** `aria-current="true"` moving funnel→stage; live screenshot shows the focused rail card with `--primary` accent border + left bar + tint, clearly distinct. Hero enter/exit (BLK-UIUX-G12-006) unchanged. 0 backend/`shared`/root-`api` diff; no new color; zh-CN intact; enum/role values unchanged; e2e strengthened (8/6 persistent items + stable order + single selection) not weakened. | | | | | |
| | **Codex return (2026-06-10):** UX revision completed first: `interaction-spec.md` B2 now defines the right rail as a persistent focus selector and adds `DEC-UX-FOCUSRAIL-01` citing this release-owner Option A direction; `design-system.md` §§6.5/7.8/10 no longer describe the resting focus rail as "other panels collapse" and records selected-state/`aria-current` using existing tokens only. Implementation: `WorkOverview.tsx` maps the full card set into `sideCards` without filtering active; `FocusStage` renders `看板选择器` with `aria-current="true"` on the selected item; `.sideCard.selected` uses existing `--primary`/`--primary-soft`/`--badge-shadow`; `overview.spec.ts` asserts manager 8-item persistent rail, stable order, selected-state transfer, switch without exit, reduced-motion retention, and sales role-scoped authorized set. Evidence: `delivery/uiux-g12-focus-rail-evidence-2026-06-10.md`. Pending Claude re-verification; Codex does not self-resolve. | | | | | |

## UI/UX Post-G12 Spot-Fix #4 — Focus header: single 返回, drop Esc hint chip — RESOLVED (release-owner design revision, 2026-06-10)

Release owner found the focus-stage header shows two redundant back affordances:
the `返回` button **and** an `Esc 返回` hint chip. Audit traced this to a
mockup-vs-impl drift (mockup `dashboard-v7-manager-focus.png` differentiates them
as `‹ 返回` button + `Esc 退出` chip; impl `WorkOverview.tsx:365-366` passes
`backLabel="返回"` + `escapeHint="Esc 返回"`, duplicating "返回"; spec line 264 also
mis-quotes the chip as "Esc 返回"). Release owner judged the **mockup itself
redundant** and directed: keep a **single 返回 button**, remove the visible Esc hint
chip; the **Esc key still exits** (functional shortcut retained, just no on-screen
hint). This overrides the locked mockup + interaction-spec B2 and needs a DEC.

| ID | Finding | Owner | Blocks | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-UIUX-G12-008 | **Focus-stage header should have one 返回 control, no Esc hint chip (release-owner design revision, supersedes mockup).** New acceptance: (1) The focus stage header top-right shows a **single `返回` button** (back-affordance; keep a back chevron per the button idiom) and **no** separate `Esc 退出`/`Esc 返回` hint chip. (2) The **Esc key still exits** the focus stage from anywhere within it (functional keyboard shortcut preserved — B6 keyboard operability must not regress); only the visible hint chip is removed. (3) The data-scope badge (`全部`/`团队`/`本人`) is unrelated and stays. (4) Approved hero enter/exit (BLK-UIUX-G12-006) and the selector rail (BLK-UIUX-G12-007) unchanged. This **supersedes the locked mockup's Esc hint chip + `interaction-spec.md` B2 lines ~261-264** ("the `Esc 返回` hint chip … stays visible"): Codex (UX) records a **DEC-UX-… decision** noting the release owner removed the redundant Esc hint chip (mockup corrected), keeps Esc functional, then implements (stop passing `escapeHint`, or render no chip). Frontend-only; 0 backend/`shared` diff; no new color; zh-CN; enum/role values unchanged; e2e strengthened to assert exactly one exit control in the header + no "Esc" hint text **and** that pressing Esc still exits; build + full e2e green, 0 skip. | Codex (UX design + execution) | UI/UX G12 focus-header re-confirm | A2/A7 (UX), B2 (locked spec/mockup revision) | **Resolved** | 2026-06-10 |
| | **Claude re-verification (2026-06-10): RESOLVED.** Independent re-audit: build PASS (`index-CrP9n7t6.css`/`index-SCNr76DL.js`); full `npm run test:e2e` PASS 49/49, 0 skip, no skip/only. Spec/DEC consistency ✓ — `interaction-spec.md` B2 + `design-system.md` revised under `DEC-UX-FOCUSEXIT-01` (removes the visible Esc hint chip, supersedes the mockup, keeps Esc functional); `escapeHint` prop **removed entirely** from `FocusStage`; `WorkOverview` passes only `backLabel="返回"`. Escape keydown exit still wired (`WorkOverview.tsx:149`). **Live browser probe** (1440px): focus header `.stageTools` has exactly **1** button (`返回`, with back chevron), `getByText(/Esc/)` count **0**; pressing Esc still exits focus → dashboard restored (confirmed with reverse-animation wait). e2e adds `expectSingleFocusExitControl` (1 tool button + 1 exact `返回` + 0 "Esc" text) across focus tests and retains the Escape-exit assertions. Hero motion (G12-006) + selector rail (G12-007) unchanged. 0 backend/`shared`/root-`api` diff; no new color; zh-CN intact; enum/role values unchanged; e2e strengthened not weakened. | | | | | |
| | **Codex return (2026-06-10):** UX revision completed first: `interaction-spec.md` B2 now removes the visible Esc hint chip and adds `DEC-UX-FOCUSEXIT-01` citing this release-owner direction; `design-system.md` §§6.5/7.11/10 align the single visible exit affordance model. Implementation: `FocusStage` no longer accepts/renders `escapeHint`; `WorkOverview.tsx` stops passing `Esc 返回`; the single `返回` button keeps a back-chevron icon and exact accessible name; existing Escape keydown exit, data-scope badge, BLK-UIUX-G12-006 hero motion, and BLK-UIUX-G12-007 selector rail remain unchanged. E2E now asserts exactly one focus-stage exit button, no visible `Esc` text, and Escape still exits. Evidence: `delivery/uiux-g12-focus-header-evidence-2026-06-10.md`. Pending Claude re-verification; Codex does not self-resolve. | | | | | |

## UI/UX Post-G12 Spot-Fix #5 — Dashboard live layer (B3 via client-side polling) — RESOLVED (release-owner direction, 2026-06-10)

Release owner reported the workbench "刷新数据" feels unresponsive and the "合并"
(自动合并) control can't be clicked. Audit found a **cluster of static/dead UI** in
`WorkOverview.tsx` `DashboardHeader` standing in for the locked **B3 live model**,
violating the core no-static-UI rule for P0/P1 surfaces:
(a) **No live mechanism at all** — no polling/SSE; the `实时更新` live-dot + `实时·…`
subtitles are decorative (one-shot load + manual refresh only).
(b) **`实时更新 / 自动合并` segmented control = two static `<span>`s** (`liveSegment`),
non-interactive (probe: `自动合并` tag = SPAN); should be the functional B3
`实时更新 / 暂停` toggle (the `LiveToggle` primitive already exists at
`components/ui/index.tsx:521`). NB the label `自动合并` conflates LIVE-03 coalescing
with the pause control.
(c) **`刷新数据`** is wired and re-fetches (probe: fires 15 requests) but has **no
visible feedback** (no button loading state, no toast, identical data → looks dead).
(d) **`本月` button** has no `onClick` — dead control.
(e) **`arrived` flash is hardcoded to `index === 0`** (always row 0), not driven by a
genuine change — cosmetic, contrary to B3 ("the only signal that says this just
changed").
Constraint reality: true server-push real-time (SSE) is an **explicitly parked future
feature** in B3 and **C1 forbids backend changes**, so the release owner elected
**client-side polling** as the interim B3 mechanism (frontend-only).

| ID | Finding | Owner | Blocks | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-UIUX-G12-009 | **Implement the locked B3 live model on the dashboard via client-side polling; remove all faked/dead live UI.** Acceptance: (1) **Polling** — the dashboard auto-refreshes by polling the existing GET endpoints it already calls in `refresh()` on a recommended ~10s cadence, **coalesced within ~800ms–1s** (DEC-UX-LIVE-03); **no backend/API/`shared` change** (C1) — existing endpoints only. (2) **Patch-in-place** — updates apply at row/value granularity with **no full-page reload**; scroll position, keyboard focus, selection, and any open Card→Focus stage/selector state are preserved across an update (never move content being read or steal focus). (3) **Change-driven `arrived` flash** — only genuinely-changed rows/values get the locked `arrived` treatment (~1.6s then fade `motion-base`); drop the hardcoded `index===0`; the underlying value is already correct without the flash (no motion-only signaling — B6); honor `prefers-reduced-motion` (no travel; value still updates). (4) **Functional `实时更新 / 暂停` toggle** — replace the static `liveSegment` with a real toggle (use/extend `LiveToggle`); `暂停` stops applying updates and the live-dot goes to a neutral paused state (no pulse); while paused, incoming changes are **buffered, not dropped**, and a non-blocking `有 N 条新更新 · 点击刷新` (zh-CN) pill appears; activating it (or resuming `实时更新`) applies the buffered batch with highlights and returns the timestamp to live; pause is a per-user/per-surface view preference that never changes server state. (5) **`刷新数据` feedback** — manual refresh shows a button loading state and updates the timestamp / a brief zh-CN confirmation. (6) **`本月`** — wire it to a real period control if existing endpoints support it frontend-only, else **remove the dead button** (do not leave a dead control; raise a blocker if a real period filter needs backend). (7) Hero motion (G12-006), selector rail (G12-007), single-返回 header (G12-008) all unchanged. Codex (UX) adds a short **DEC** noting the interim live mechanism = client-side polling (SSE remains the parked future path). Frontend-only; 0 backend/`shared`/root-`api` diff; **no new color** (paused dot + arrived use existing/locked tokens); zh-CN; enum/role comparison values unchanged. e2e strengthened: assert polling applies an update without full reload, `arrived` only on changed rows, the toggle is a real control that pauses (buffer + `有 N 条新更新` pill) and resumes, and `刷新数据` shows feedback; build + full e2e green, 0 skip. | Codex (UX design + execution) | UI/UX G12 dashboard-live re-confirm | B3 (locked live model), LIVE-03, no-static-UI core rule, C1/C6 | **Resolved** | 2026-06-10 |
| | **Claude re-verification (2026-06-10): RESOLVED — B3 live realized via polling; all faked/dead UI removed.** Independent re-audit: build PASS (`index-93NodPN2.css`/`index-D3zHi2KO.js`); full `npm run test:e2e` PASS **50/50** (+1), 0 skip, no skip/only. Static `liveSegment`/`自动合并` span **gone**; dead `本月` button **removed** (remaining `本月` strings are MetricCard labels); `arrived` is now **change-driven** (`changedKeys.has(...)`, no more `index===0`); `DEC-UX-LIVE-04` records polling as the interim mechanism (SSE parked). New **`TEST-UIUX-B3-001`** is a real behavioral test (injects changing activity data via route intercept): asserts polling applies the update in place + `arrived` on the changed row only; pausing buffers (new data not shown) + shows `有 N 条新更新 · 点击刷新` pill + paused live-dot + toggle `aria-pressed=false`; resuming applies the buffered batch with `arrived`. **Live browser probe** (1440px): ~4 polling requests over 12s (auto-refresh cadence); clicking `实时更新` → paused dot visible + `aria-pressed=false`; `刷新数据` now shows feedback (`已刷新 · HH:MM`). Hero motion (G12-006) + selector rail (G12-007) + single-返回 header (G12-008) unchanged. 0 backend/`shared`/root-`api` diff; no new color; zh-CN intact; enum/role values unchanged; e2e strengthened (real polling/pause/buffer/pill behavior) not weakened. | | | | | |
| | **Codex return (2026-06-10):** UX decision completed first: `interaction-spec.md` B3 now adds `DEC-UX-LIVE-04`, binding dashboard live to client-side polling of existing GET endpoints while keeping SSE parked. Implementation: `WorkOverview.tsx` now has 10s polling + 900ms coalescing, patch-in-place snapshot application, pause buffering with `有 N 条新更新 · 点击刷新`, manual `刷新中`/`已刷新` feedback, removed dashboard `本月` dead button, removed static `实时更新/自动合并` spans, and replaced hardcoded row-0 `arrived` with diff-driven payment/activity row keys. `LiveToggle` now renders `实时更新`/`暂停`; paused dot and buffer pill use existing tokens only. E2E adds `TEST-UIUX-B3-001` for polling/no reload, change-only arrived, pause buffer/resume, and refresh feedback; full e2e is 50/50. Evidence: `delivery/uiux-g12-dashboard-live-b3-evidence-2026-06-10.md`. Pending Claude re-verification; Codex does not self-resolve. | | | | | |

## UI/UX Post-G12 Spot-Fix #6 — Dashboard responsive break at narrower desktop widths (A6) — RESOLVED (release-owner review, 2026-06-10)

Release owner review: at full-screen the dashboard is mostly OK but some text
clips/overflows; at a **non-fullscreen desktop window (~1200–1400px)** the dashboard
**deforms badly** — donut legend labels wrap **character-by-character** (新/商/机
stacked vertically), KPI value `¥12,248,345` overlaps its icon, cards are crushed.
Root cause: the dashboard card grid has **no responsive column reduction** — the
`@media (max-width:1199px)` block collapses the sidebar + other grids but **never
touches `.managerDashboardGrid`/`.salesDashboardGrid`**, which stay
`repeat(4|3, minmax(0,1fr))` at every width; in the 1200–1440 band the 248px sidebar
+ 4 columns leaves each card too narrow. Contributing: `.legendItem` middle column
is `minmax(0,1fr)` with no `white-space:nowrap` → CJK labels char-wrap when cramped;
KPI metric value has no shrink/overflow guard vs its icon. This violates A6 ("桌面优先
+ 响应式不破版:合理宽度区间不错位/不溢出"). The prior `TEST-UIUX-A6-001` only checked
1440/1680/900px and missed the 1200–1400 band that breaks.

| ID | Finding | Owner | Blocks | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-UIUX-G12-010 | **Dashboard must degrade gracefully across the reasonable desktop width range (A6) — no overflow/overlap/char-wrap.** Acceptance: (1) Add responsive column reduction for the dashboard card grid + top KPI strip so cards keep a usable min width — e.g. manager 4→2 (and KPI strip 4→2) and sales 3→2 as width narrows; pick breakpoints that keep the 1200–1440 band clean (the current 1199 cut is too low and skips that band). (2) **Donut legend** must not wrap CJK labels character-by-character — `white-space:nowrap` on the label (with ellipsis if needed) and/or stack the legend below the donut at narrow card widths. (3) **KPI metric value** must not overlap/clip its icon — value shrinks/wraps gracefully, icon `flex-shrink:0`, min-width handled. (4) No text clipping/overflow/overlap anywhere on the dashboard from ~**1024px up to ultra-wide** (desktop-first; full mobile breakpoints remain out of scope per A6, but below ~1024 must degrade cleanly, not catastrophically — single column acceptable). (5) Keep 1440px primary look intact; hero motion (G12-006), selector rail (G12-007), single-返回 header (G12-008), B3 live (G12-009) all unchanged. Frontend-only; 0 backend/`shared` diff; **no new color**; zh-CN; enum/role values unchanged. e2e strengthened: `TEST-UIUX-A6-001` (or a new case) asserts **no per-card scroll/client overflow at representative intermediate widths — at least 1280px, 1180px, and 1024px** (the band the current test skips), in addition to existing 1440/1680/900. Build + full e2e green, 0 skip. | Codex (UX design + execution) | UI/UX G12 dashboard-responsive re-confirm | A6 (responsive/no-break), C6 | **Resolved** | 2026-06-10 |
| | **Claude re-verification (2026-06-10): RESOLVED.** Independent re-audit: build PASS (`index-adHxixKd.css`/`index-CC1VeCcU.js`); full `npm run test:e2e` PASS 50/50, 0 skip, no skip/only. CSS-only fix (0 backend/`shared` diff): `@media(max-width:1439px)` drops `.dashboardKpis` + `.managerDashboardGrid`/`.salesDashboardGrid` to 2 columns; `@media(max-width:1079px)` → 1 column; legend label `min-width:0; overflow:hidden; text-overflow:ellipsis; white-space:nowrap`; KPI `.metricTile>div min-width:0` + value `overflow-wrap:anywhere` + icon `flex:0 0 auto`. `TEST-UIUX-A6-001` now checks 1680/1440/**1280/1180/1024**/900 for per-card + page overflow. **Live browser probe** at the previously-broken band (1280/1180/1024px): per-card overflow `[]`, page horizontal overflow `0`, zero legend labels taller than 30px (no char-wrap); 1180px screenshot shows clean 2-col layout — legend single-line, KPI value not overlapping its icon. 1440px primary look intact; hero (G12-006) + selector rail (G12-007) + single-返回 header (G12-008) + B3 live (G12-009) unchanged. No new color; zh-CN intact; enum/role values unchanged; e2e strengthened (intermediate widths) not weakened. | | | | | |
| | **Codex return (2026-06-10):** dashboard grids now degrade frontend-only: KPI strip 4→2→1, manager grid 4→2→1, sales grid 3→2→1; donut legend labels are nowrap/ellipsis; KPI tiles and dashboard rows now have min-width/ellipsis protections so long amounts, account IDs, and badges do not overlap or create scroll overflow. `TEST-UIUX-A6-001` now checks 1680/1440/1280/1180/1024/900 widths, including KPI tiles, per-card scroll/client overflow, legend wrap, KPI/icon overlap, and document horizontal overflow. Evidence: `delivery/uiux-g12-dashboard-responsive-a6-evidence-2026-06-10.md`. Pending Claude re-verification; Codex does not self-resolve. | | | | | |

## UI/UX Post-G12 Spot-Fix #7 — Dashboard card text over-truncation + icon overlap — RESOLVED (release-owner review, 2026-06-10)

Release owner: resize is fixed, but content is occluded — specifically pointed at the
**团队回款到账 (payments)** and **团队最近活动 (activity)** cards (and the donut legend is
also affected). The G12-010 fix traded character-wrap for `text-overflow:ellipsis` on
dashboard card text, which now **over-truncates readable content** whenever a 4-column
card is cramped in the **1440–~1680px band** (4 cols, ~290px/card). Measured at 1512px
(release-owner screen): **payment-row meta `137 条 · 应收 ¥…` is cut** (clientWidth 79 <
scrollWidth 84 → the 应收 amount is clipped); **donut legend** labels truncate to ~2
chars (新.. 需.. 合.. 丢..) — 1440px = all 6 legend labels, 1512px = 4, 1600px = 1,
1700px = none. (Activity card title/meta/time measured not truncated at 1512px, but is
included for safety.) ≥1700px and ≤1439px (2-col) are clean; the 1440–1680 band breaks.
The G12-010 e2e checked overflow/char-wrap height but **not ellipsis truncation**
(`scrollWidth>clientWidth`), so it passed.

| ID | Finding | Owner | Blocks | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-UIUX-G12-011 | **Dashboard card text must render in full (no ellipsis truncation) at all desktop widths ≥1024px — fix the G12-010 over-truncation in the 1440–1680px 4-col band.** Acceptance: (1) **团队回款到账 payment rows** — the meta line (`N 条 · 应收 ¥amount`) and the money/status must show **in full, no `…`**, at every desktop width ≥~1024px (currently the 应收 amount is clipped at ~1440–1600px). (2) **Donut legend** labels (新商机/需求已确认/报价/合同谈判/赢单/丢单) show **in full, no `…`** at every desktop width ≥~1024px including the 1440–1680px band where the side-by-side `.donutShell` (156px donut + legend) currently can't fit — recommended: stack the legend below the donut at constrained widths (full card width) and/or shrink donut / tighten legend; `text-overflow:ellipsis` only as last-resort below ~1024px, not at normal desktop widths. (3) **团队最近活动 activity rows** — title/meta/time render without clipping or overlap across the same range. (4) General principle: the dashboard's `text-overflow:ellipsis`/min-width guards from G12-010 must protect against *overflow* without truncating readable text at normal desktop widths — prefer reflow/room/stack over ellipsis for financial amounts and short CJK labels. (5) No regression to G12-010 column reductions or 1440 primary look; hero (G12-006) + selector rail (G12-007) + header (G12-008) + B3 live (G12-009) unchanged. Frontend-only; 0 backend/`shared` diff; **no new color**; zh-CN; enum/role values unchanged. e2e strengthened: assert **no ellipsis truncation** (`scrollWidth ≤ clientWidth + 1`) on payment-row meta, donut legend labels, and activity rows at representative widths including **1440/1512/1600** (the break band) plus existing checks. Build + full e2e green, 0 skip. | Codex (UX design + execution) | UI/UX G12 dashboard-text re-confirm | A6 (responsive/no-break), C6 | **Resolved** | 2026-06-10 |
| | **Codex return (2026-06-10):** fixed the G12-010 over-truncation frontend-only. At desktop widths `>=1024px`, dashboard card row text uses reflow instead of ellipsis; in the constrained 1024–1699px band, donut legend stacks below the ring and payment/activity right-side amount/status/time move onto the content column so meta/title remain readable. `TEST-UIUX-A6-001` now covers 1680/1600/1512/1440/1280/1180/1024/900 and adds no-truncation checks (`scrollWidth <= clientWidth + 1`) for payment meta/amount/status, donut labels, and activity title/meta/time at `>=1024px`. Evidence: `delivery/uiux-g12-dashboard-text-truncation-evidence-2026-06-10.md`. Pending Claude re-verification; Codex does not self-resolve. | | | | | |
| | **Claude re-verification (2026-06-10): FAIL — over-truncation fixed, but a new icon/text OVERLAP was introduced.** Build PASS (`index-DY0vDZnW.css`). The text-truncation part is good (text now reflows, no ellipsis). BUT the new `@media(1024–1699px)` 2-column `.paymentRow`/`.event` uses `grid-template-columns: 22px minmax(0,1fr)` while `.flowIcon` is actually **42px** → the icon overflows its 22px track ~20px and overlaps the row text. Live overlap probe at 1512px: 团队最近活动 `icon_over_text = true` on **all 4 rows** (gridCols `22px 225px`, iconW `42`); 团队回款到账 has the same 42px-icon-in-22px-column root cause. This is exactly the "文字和图形遮挡" the release owner still sees after hard refresh. **Fix: set the 2-col icon track to the real icon width (~42–44px), not 22px** — e.g. `.paymentRow`/`.event { grid-template-columns: 42px minmax(0,1fr) }` in that media block (the 22px legend swatch is a different element — leave it). Add an e2e **overlap** assertion (icon bounding-box must not intersect the row text) for payments + activity at 1440/1512/1600, alongside the existing no-truncation checks. Returned to Codex; G12-011 remains unresolved. | | | | | |
| | **Codex return after re-kick (2026-06-10):** corrected the base and 1024–1699px payment/activity row icon tracks to the real `.flowIcon` width: `.paymentRow` and `.event` now use 42px icon tracks for both manager and sales dashboards. `TEST-UIUX-A6-001` now adds a bounding-box overlap assertion for `.flowIcon` versus same-row text/content in payment rows and activity rows, covering 1700/1680/1600/1512/1440/1280/1180/1024/900; the prior no-truncation assertions remain. Verification: overview e2e PASS 7/7; `npx tsc --noEmit` PASS; `npm run build` PASS (`index-Dw0l66tN.css`, `index-Co62K16j.js`); full `npm run test:e2e` PASS 50/50, 0 skipped; no skip/only; `git diff --check` PASS; 0 backend/`shared`/root-api diff/status; no new color literals. Evidence updated: `delivery/uiux-g12-dashboard-text-truncation-evidence-2026-06-10.md`. Pending Claude re-verification; Codex does not self-resolve. | | | | | |
| | **Claude re-verification #2 (2026-06-10): RESOLVED.** Icon track corrected to `42px minmax(0,1fr)` for `.paymentRow`/`.event` (manager + sales). Build PASS (`index-Dw0l66tN.css`); full `npm run test:e2e` PASS 50/50, 0 skip (one A7 reduced-motion flake under parallel load passed on isolated rerun + a clean full re-run — pre-existing flake, not this change). **Live overlap+truncation probe at 1440/1512/1600** (the break band): 团队回款到账 and 团队最近活动 both `overlaps=0, truncs=0` at every width — icon no longer overlaps text and nothing is ellipsis-truncated. Donut legend full. New e2e `expectDashboardFlowRowsDoNotOverlap` asserts `.flowIcon` bbox does not intersect same-row text across 1700→900, alongside the no-truncation checks. 0 backend/`shared`/root-`api` diff; no new color; zh-CN intact; enum/role values unchanged; prior spot-fixes (G12-006/007/008/009/010) unchanged. **Watch item (not blocking):** `TEST-UIUX-A7-001` reduced-motion flaked once under full-suite parallel load (passed isolated + on full re-run) — pre-existing reliability nit to stabilise. | | | | | |

## UI/UX Post-G12 Spot-Fix #8 — Slow the overview→focus hero transition 320→450ms — RESOLVED (release-owner direction, 2026-06-10)

Release owner finds the **overview→focus hero entrance (英雄登场)** too fast and wants it
slower. Current durations: hero **enter = 320ms** (`dashboardStageEnter` on
`--motion-slow`; JS `focusEnterMs=320`), reverse = 220ms (`--motion-base`; `focusExitMs=220`),
rail-card switch = 220ms (`focusSwitchMs=220`). Release owner chose **enter → 450ms**
(reverse scales proportionally, still quicker than enter; rail switch unchanged). This
**exceeds the locked B1 `motion-slow` 320ms cap** (B1: "if a motion would need longer
than motion-slow it is too big" + motion-slow reserved for this hero transition), so it
is a release-owner-directed motion revision needing a DEC.

| ID | Finding | Owner | Blocks | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-UIUX-G12-012 | **Slow the Card→Focus hero ENTER transition from 320ms to ~450ms (reverse proportional ~310ms; rail switch unchanged at 220ms).** Acceptance: (1) Overview→focus **hero enter completes in ~450ms** — the full B2 choreography (scrim, clicked-card FLIP into the stage, content compact→full crossfade, other-panels stagger-collapse, sidebar collapse) scales to the new ~450ms window and stays well-sequenced (no element finishing jarringly early/late). (2) **Reverse (返回/Esc) ~310ms** — proportionally slower than today but still **quicker than enter** (preserve B2's "exit quicker than enter" feel). (3) **Rail-card switch stays 220ms** (`focusSwitchMs`/`dashboardStageSwitch` on `--motion-base`) — must NOT inherit the slower hero timing. (4) **reduced-motion still snaps** (no travel, ~80ms) — B6 path unchanged. (5) **CSS animation durations and JS phase-timeout constants stay in sync** — the hero enter/exit `@keyframes` durations + strip stagger delays and `focusEnterMs`/`focusExitMs` must match so the motion-phase state and the visual animation end together (no premature snap or lingering frozen frame). Use a dedicated hero-duration token/value (do not repurpose `--motion-base`, which the 220ms switch + other micro-motions still use). (6) Codex (UX) records a **DEC** noting the hero transition duration is raised to ~450ms above the locked `motion-slow` 320ms per this release-owner direction (only the hero enter/exit; the B1 scale otherwise stands). (7) Hero choreography content (G12-006), selector rail (G12-007), single-返回 header (G12-008), B3 live (G12-009), responsive (G12-010/011) all unchanged except the enter/exit duration. Frontend-only; 0 backend/`shared` diff; **no new color**; zh-CN; enum/role values unchanged. e2e: `TEST-UIUX-A7-001` still passes (animation names unchanged); keep build + full e2e green, 0 skip. | Codex (UX design + execution) | UI/UX G12 hero-timing re-confirm | A7 (motion), B1/B2 (locked timing revision) | **Resolved** | 2026-06-10 |
| | **Claude re-verification (2026-06-10): RESOLVED.** Build PASS (`index-hY67B0Hc.css`); full `npm run test:e2e` PASS 50/50, 0 skip (no A7 flake this run). `DEC-UX-HEROTIME-01` recorded; new `--motion-hero:450ms`/`--motion-hero-exit:310ms` tokens are **injected from the JS `focusEnterMs`/`focusExitMs` constants** (652-653) so CSS and JS share one source (guaranteed in sync); `dashboardStageSwitch` still uses `--motion-base`. **Live timing probe** (1440px): `--motion-hero=450ms`/`--motion-hero-exit=310ms`/`--motion-base=220ms`; measured stage animation — ENTER stage anim 0.41s (=450−40ms delay → 450ms total window) ✓, **SWITCH 0.22s = 220ms (rail switch unchanged)** ✓, EXIT 0.31s = 310ms ✓. e2e `TEST-UIUX-A7-001` now asserts real animation totals StageEnter≈450 / StageExit≈310 / StageSwitch≈220 (anti-regression). reduced-motion unchanged (focusReducedMs=80). 0 backend/`shared`/root-`api` diff; no new color; zh-CN intact; enum/role values unchanged; G12-006..011 unchanged except enter/exit duration. | | | | | |
| | **Codex return (2026-06-10):** UX DEC landed first: `interaction-spec.md` B1/B2 now records `DEC-UX-HEROTIME-01`, preserving the 80/140/220/320ms B1 scale while carving out dedicated Card→Focus hero timing (~450ms enter / ~310ms reverse); `design-system.md` references the DEC and confirms selector switching is unaffected. Implementation: `--motion-hero:450ms` and `--motion-hero-exit:310ms` added as dedicated tokens; `focusEnterMs=450`, `focusExitMs=310`, `focusSwitchMs=220`, `focusReducedMs=80`; stage/content/scrim/strip enter-exit animations are scaled so the last manager strip enter ends at 450ms and the last strip exit ends at 310ms; `dashboardStageSwitch` stays on `--motion-base` 220ms and reduced motion remains opacity-only. `TEST-UIUX-A7-001` now records animation duration+delay totals and asserts StageEnter ~=450ms, StageExit ~=310ms, StageSwitch ~=220ms while retaining named-animation, rail, header, focus-restore, and reduced-motion coverage. Verification: A7 targeted e2e PASS 1/1; overview e2e PASS 7/7; `npx tsc --noEmit` PASS; `npm run build` PASS (`index-hY67B0Hc.css`, `index-qO7se2PA.js`); full `npm run test:e2e` PASS 50/50, 0 skipped; no skip/only; 0 backend/`shared`/root-api diff/status; no new color literals. Evidence: `delivery/uiux-g12-hero-timing-evidence-2026-06-10.md`. Pending Claude re-verification; Codex does not self-resolve. | | | | | |

## Open UI/UX Execution Blockers

These blockers are surfaced during the 2026-06-07 UI/UX completion G9 execution
attempt after Claude passed the UI/UX G8 handoff audit. Per the G8 yardstick,
Codex may not silently recolor locked tokens or weaken accessibility.

| ID | Blocker | Owner | Blocks | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-UIUX-G9-001 | A5 accessibility requires WCAG AA text contrast, while C6 locks the approved `docs/ux-ui/design-system.md` palette and forbids recolor/new tokens. Codex measured locked token/background pairs before implementing UIUX-001 and found multiple AA failures: `--subtle #94A3B8` on `--card #FFFFFF` = 2.56; `--subtle #94A3B8` on `--section #F6F7FD` = 2.40; `--success #16A34A` on `--mint-soft #E5F7F0` = 2.97; `--warning #D97706` on `--peach-soft #FDEDE5` = 2.79; `--danger #DC2626` on `#FEE2E2` = 3.95 for normal text; `--purple #B79CF0` on `--purple-soft #F2ECFD` = 2.02. The yardstick's 2026-06-07 A5↔C6裁决 says this exact case must kick back to Claude; Codex must not silently change colors or proceed with known non-AA text pairings. Claude needs to decide whether to approve minimal contrast-only token exceptions, clarify which locked pairings are non-text/large-only/decorative, or revise the yardstick/design-system. | Claude (Product Manager / UI / Accessibility yardstick owner) | UI/UX G9 implementation start; UIUX-001 and all downstream UIUX-002..014 | ACC-018, ACC-023; A5; C6 | **Resolved** | 2026-06-07 |
| | **Resolution (2026-06-07, user 选定方案 A → DEC-UIUX-A5-001, recorded in `docs/ux-ui/requirements/uiux-implementation.requirements.md` A5):** Approved a strictly-bounded **contrast-only text-token exception**. Codex (UIUX-001) adds minimal darker *text-shade* tokens (e.g. `--success-ink/--warning-ink/--danger-ink/--purple-ink`) — each the locked hue darkened the minimum to reach AA (≥4.5:1 normal / ≥3:1 large) on the backgrounds it renders on; readable secondary text uses existing `--muted #475569`; `--subtle #94A3B8` reclassified non-text/decorative/disabled only (WCAG 1.4.3 exempt). **All backgrounds/soft tints/solid fills/brand/borders/icon colors stay byte-for-byte locked (C6)** — exception is text-legibility ONLY, never a fill/background/chip color. Codex must deliver exact hex + recomputed WCAG ≥AA table + token diff proving additions-only, append the decision as an accessibility addendum to `design-system.md`, and return to Claude for token re-audit before downstream UIUX work; final verification folded into G12. G9 unblocked. | | | | | |
| | **Codex token checkpoint evidence (2026-06-07):** submitted `delivery/uiux-token-exception-a5-2026-06-07.md` and `docs/ux-ui/design-system.md` Appendix A with exact text-only token hexes, WCAG AA recomputation, previous-lighter-hex minimality proof, and additions-only token diff. `planning/gate-status.md` UI/UX G9 is `Gate Review` pending Claude token re-audit; remaining UIUX-001 implementation and UIUX-002..014 stay held. |
| | **Claude token re-audit (2026-06-07): PASS.** WCAG math independently recomputed (all four `*-ink` ≥4.5:1 on card/section/soft; `--muted` ≥7) and matches Codex to 0.01; minimality confirmed (next-lighter hex 4.47–4.49 < AA); `design-system.md` change additions-only (every locked background/fill/brand/border value byte-unchanged; deletions are usage rewrites + text→`*-ink` swaps; fills stay locked); `*-ink` text-only, `--subtle` decorative, secondary text → `--muted`; no code/backend touched. Downstream UIUX-002..014 + remaining UIUX-001 foundation released; final per-page verification folded into G12. | | | | | |

## Open UI/UX G12 Rework — Phase 1 Touch-up Findings (2026-06-08)

Phase 1 (CRUD archetypes) re-audit: the G12 list-archetype FAIL is RESOLVED (all 8
lists rebuilt to the locked data table; bulk actions wired to real existing backend
endpoints; 0 backend diff; no new colours; zh-CN; e2e 45/45/0-skip; build green).
Two affordance defects remain — frontend-only touch-up before Phase 2.

| ID | Finding | Owner | Blocks | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-UIUX-P1-001 | **Opportunity detail missing/dead affordances (A2/A4 + yardstick line 45).** `frontend/src/pages/opportunities/OpportunityDetail.tsx` header `编辑` button has no `onClick` (dead control); `归档` and `转移负责人` actions are absent entirely. Locked mockup `detail-opportunity.png` and `docs/ux-ui/requirements/batch-archetypes.requirements.md` line 45 require 编辑/转移负责人/归档 (Manager/Admin gated; Sales restricted; terminal records stay read-only). Backend endpoints exist and are already wired from the list (`/opportunities/{id}/archive`, edit via `UpdateOpportunity` incl. reassign for Manager/Admin). Fix frontend-only: wire 编辑 → opportunity form in edit mode; add 归档 (Manager/Admin, calls existing archive endpoint) and 转移负责人 (Manager/Admin) to the detail header with A4 gating; keep terminal read-only. | Codex (execution) | Phase 2 start | A2, A4 | **In Review** | 2026-06-08 |
| | **Codex touch-up return (2026-06-08):** detail `编辑` now opens the real edit form; `转移负责人` uses existing `PATCH /api/opportunities/{id}`; `归档` uses existing `/api/opportunities/{id}/archive`; Sales hides edit/transfer/archive; terminal records hide those entry points. Evidence: `delivery/uiux-phase1-touchup-evidence-2026-06-08.md`. Pending Claude quick re-audit; Codex does not self-resolve. | | | | | |
| | **Claude re-audit (2026-06-08): RESOLVED.** Verified in code + live detail screenshot: all three actions render only under `canManage` (Administrator/Sales Manager) → Sales-hidden (yardstick line 45); terminal Won/Lost shows only disabled close buttons (read-only); `PATCH /opportunities/{id}` + `/archive` handlers confirmed to exist; 0 backend diff; e2e 47 passed/0-skip; build green. | | | | | |
| BLK-UIUX-P1-002 | **Create-form stage locked incorrectly (yardstick line 66; functional reduction).** `frontend/src/pages/opportunities/OpportunityList.tsx` renders the create-form 阶段 as a disabled `<select>` fixed to 新商机 and create hardcodes `stage:'New Opportunity'`. Yardstick line 66 requires the form to list the **4 non-terminal stages** (新商机/需求已确认/报价/合同谈判), default 新商机, excluding terminal (赢单/丢单). Backend `services/opportunity/internal/domain/opportunity.go` `CreateOpportunity` accepts a passed `Stage` validated by `IsStage` — so the lock is NOT a domain necessity; it is a functional reduction vs both yardstick and backend capability. Fix frontend-only: render the 4 non-terminal stages as an enabled selectable control (default 新商机, terminal excluded) and send the chosen stage to `createOpportunity` (stop hardcoding). | Codex (execution) | Phase 2 start | A2; yardstick line 66 | **In Review** | 2026-06-08 |
| | **Codex touch-up return (2026-06-08):** create/edit form stage is now an enabled four-option non-terminal chip group, defaults to New Opportunity, excludes Won/Lost, and submit sends the selected `stage` to `createOpportunity` or `updateOpportunity`. Evidence: `delivery/uiux-phase1-touchup-evidence-2026-06-08.md`. Pending Claude quick re-audit; Codex does not self-resolve. | | | | | |
| | **Claude re-audit (2026-06-08): RESOLVED.** Verified code (`editableStages` = 4 non-terminal, chip group with `aria-pressed`, `createOpportunity({...form})` sends selected stage) + live form screenshot (chips 新商机/需求已确认/报价/合同谈判, default 新商机, terminal excluded). 0 backend diff; build/e2e green. | | | | | |

## Open UI/UX G12 Audit Findings (rework #1)

Surfaced by Claude's independent UI/UX G12 audit 2026-06-07 (substantive PASS; two
display-layer defects elected for fix-before-pass). Frontend-only; no backend change.
Package: `delivery/uiux-g12-rework-1.md`.

| ID | Finding | Owner | Blocks | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-UIUX-G12-003 | **Design not realized — G12 FAIL (MAJOR, systemic).** Per-page fidelity audit (live 1440px screenshots of all 14 nav pages vs `docs/ux-ui/mockups/*.png`, 4 parallel agents) found **every page is a MAJOR deviation** from the locked design. The implementation applied the design-SYSTEM layer (tokens/shell/nav/role-gating/zh-CN — these pass) but did NOT build the page DESIGNS: dashboard = 3 tiles + global two-mode toggle vs 8 cards + per-card Card→Focus (§10/A7); all 8 list pages = old two-pane master-detail card list vs locked multi-column filterable paginated data table; reports/admin/reminders/import-export/operation-log all missing their designed structures. Violates A2 (page→mockup realization), A7 (Card→Focus), C6 (mockup consistency). Supersedes the "substantive PASS" first-pass verdict (which omitted the A2/C6 per-page check). | Codex (execution) | UI/UX G12 pass | A2, A7, C6 | **Resolved** 2026-06-08 | 2026-06-08 |
| | **Resolution (2026-06-08, phased rebuild P0→P3 + touch-ups, Claude G12 closing sweep PASS):** all 14 nav pages rebuilt to the locked mockups (dashboard 8-card + per-card Card→Focus; 8 CRUD list/detail/form data-table archetype; reports/admin/reminders/import-export/operation-log designed structures). Cumulative: 0 backend diff; no colour outside locked+`*-ink`; zh-CN intact; e2e 3× consecutive 49/49, 0 skip, no assertion weakening; build green. A2/A7/C6 satisfied. See gate-status UI/UX G12 = Gate Passed. | | | | | |
| BLK-UIUX-G12-001 | **C3 breach (MINOR, binding) — folds into rebuild Phase 3.** `frontend/src/pages/importexport/Export.tsx:75` renders `result.fileSafety` raw; backend value is `dangerous_cells_prefixed` (`services/import-export/internal/handler/export_metadata.go:21`) — a new English token shown to zh-CN users, regressing zh-CN. Fix frontend-only: add `fileSafetyLabel` to `frontend/src/i18n/labels.ts` (cover all backend `fileSafety` values), render via `labelFor` with safe raw fallback; do not change backend or the API type. | Codex (execution) | UI/UX G12 pass | C3, A9 | **Resolved** 2026-06-08 (Phase 3) | 2026-06-07 |
| | **Resolution (2026-06-08, Phase 3):** `fileSafetyLabel` added to `frontend/src/i18n/labels.ts` (`dangerous_cells_prefixed: '危险单元格已安全前缀化'`); `Export.tsx` renders via `fileSafetyText()` → `fileSafetyLabel[value] ?? '文件安全状态已记录'` (safe zh-CN fallback). Display-only; backend/API type unchanged. Verified in code + live screenshot. | | | | | |
| BLK-UIUX-G12-002 | **A3 (MINOR) — folds into rebuild Phase 1 (list archetype).** `frontend/src/pages/opportunities/OpportunityList.tsx:115-124` renders `批量转移负责人`/`批量归档` for Manager/Admin but hardcoded `disabled`; no bulk endpoint exists in the backend (confirmed) and there is no A3 explanation of the missing requirement. Not a downgrade (never shipped) and a backend must NOT be added (C1). Fix frontend-only: remove the dead buttons, or keep with an explicit zh-CN disabled-reason/helper per A3. Keep Sales-hidden gating intact. | Codex (execution) | UI/UX G12 pass | A3, C1 | **Resolved** 2026-06-08 (Phase 1) | 2026-06-07 |
| | **Resolution (2026-06-08, Phase 1 re-audit):** the list bulk affordances are now functional — `批量归档` wired to the existing `/archive` endpoint (per-record over selected rows) with `清除选择`, gated to Manager/Admin and Sales-hidden; no dead disabled placeholder remains; 0 backend diff. Superseded the "no bulk endpoint" framing by composing existing single-record endpoints over the selection. | | | | | |
| | **Codex Phase 1 return (2026-06-08):** CRUD list archetypes now use `BulkActionBar` across the rebuilt entity lists. Sales users do not render bulk transfer/archive affordances per A4. Manager/Admin bulk actions use existing single-record frontend operations where existing backend endpoints support the action; unavailable actions remain disabled with zh-CN A3 reasons. Evidence: `delivery/uiux-phase1-crud-archetypes-evidence-2026-06-08.md`. Status is In Review pending Claude screenshot-vs-mockup re-audit; Codex does not self-resolve. | | | | | |

## Open Modeling Decisions — must close before G8

These were surfaced by the G6 MDA modeling (CIM/PIM) and its independent multi-agent
audits. Each is a genuine upstream-source gap that MDA correctly declined to invent
(per the no-invention discipline).

> **Update 2026-06-01: ALL THREE RESOLVED by a Formal Scope Change by User**
> (decision-log.md DEC-017..020). BLK-001 → DEC-020 (Opportunity Status field
> removed); BLK-002 → DEC-017/019 (Won = contract Signed; payment decoupled, so no
> multi-plan "fully paid" Won-gate); BLK-003 → DEC-018 (one quote per opportunity;
> no second quote to accept). Cascade applied across baseline → architecture → MDA
> and re-audited. See Resolution Log below.

| ID | Blocker | Owner | Blocks | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-001 | Opportunity Status enumerated value set distinct from Pipeline Stage is not enumerated in the accepted sources (CIM-016 commits Status as a persisted dimension; PRD-007/ACC-007 require status as a persisted field but enumerate no values). Decide whether a distinct Status enumeration exists or whether Status is realized through Stage. | Product Manager + Business Analyst | G8 handoff (and G7 test design determinism) | ACC-007 (P0) | Resolved — Formal Scope Change by User (DEC-020) | 2026-06-01 |
| BLK-002 | Multi-plan contract "fully paid" aggregation is undefined. Overpayment ceiling is fixed at contract level (EDGE-019) and Won requires contract-level full payment (BR-008, DEC-012), but the accepted sources do not define how multiple Payment Plans under one contract aggregate into "contract fully paid." Confirm the aggregation rule. | Product Manager + Business Analyst | G8 handoff (and G7 deterministic Won/overpayment test design) | ACC-011, ACC-013 (P0) | Resolved — Formal Scope Change by User (DEC-017/019) | 2026-06-01 |
| BLK-003 | Second-quote-accept observable outcome is unspecified. The "at most one Accepted quote per opportunity" invariant is fixed, but EDGE-012 does not specify the observable business result of accepting a second quote (reject the second accept vs auto-demote the current Accepted quote). A product decision is required before deterministic test design. | Product Manager | G7 test design / G8 handoff | ACC-009 (P0) | Resolved — Formal Scope Change by User (DEC-018) | 2026-06-01 |

### Source pointers
- BLK-001: `modeling/CIM.md` CIM-016; `modeling/PIM.md` PIM-SM-003, PIM-OPEN-003; `docs/product/acceptance-matrix.md` ACC-007; PRD-007.
- BLK-002: `modeling/PIM.md` PIM-SM-006, PIM-INV-007/023/025, PIM-OPEN-005; `docs/business/business-rules.md` BR-008; `docs/product/decision-log.md` DEC-012; `docs/business/edge-cases.md` EDGE-019.
- BLK-003: `modeling/CIM.md` CIM-PROC-008 Open/Blocked; `modeling/PIM.md` PIM-SM-004, PIM-OPEN-001; `docs/business/edge-cases.md` EDGE-012; ACC-009.

## Architecture / PSM-deferred (not PM/BA blockers; tracked for G7/PSM)

These are correctly deferred to PSM/Architecture at PIM altitude and are recorded for
the PSM artifact and G7 test design; they are not pre-G8 PM/BA decisions.

| ID | Item | Owner | Resolves at | Refs |
|---|---|---|---|---|
| BLK-A01 | Overdue-evaluation trigger (on-read vs scheduled) — needed for deterministic overdue test design; modeled at PIM as a Business-Date guard, mechanism deferred. | Architecture (in PSM) | PSM / G7 | PIM-OPEN-002, CIM-034, BR-021 |

## Open G11 Release Evidence Blockers

These are active execution blockers surfaced during Codex G9-G11 execution. Per the
no-invention discipline, Codex may not invent production endpoint, TLS, security-group,
monitoring, or runtime evidence.

| ID | Blocker | Owner | Blocks | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-G11-001 | TASK-039 cannot be completed because production release requires a valid HTTPS endpoint with TLS certificate, but the deployment source says the domain is "Not specified yet" and absence of a valid HTTPS endpoint blocks production release. G11 also requires real runtime evidence on `srv-volcengine-sh-01` for endpoint, TLS status, security-group inbound rules, opened ports, health URL, deployment timestamp, operator, and smoke result. | Infrastructure Ops + Integration Owner | G11 handoff to G12; TASK-039 | ACC-017 (P0), ARCH-ACC-008/013/014/015, TEST-DEPLOY-SMOKE-001/002 | Resolved — `https://118.196.44.193` approved and provisioned with a Let's Encrypt IP certificate on 2026-06-03 | 2026-06-03 |
| BLK-G11-002 | TASK-039 has server-side HTTPS/TLS, health, redirect, service health, monitoring threshold, renewal, operator evidence, and Volcengine security-group API evidence. The API evidence binds instance `i-yemoz0an7kk36d2c9bp6` to security group `sg-1pm4k7f37z8xs643rg0fvk85e` through ENI `eni-13e8tbocd8f0g79iu5jer8idt` and confirms CRM gateway `8080` and PostgreSQL `5432` are not allowed from `0.0.0.0/0`. User approved releasing previous deployments; Codex stopped and removed host-network Hermes, so host-level `8642` no longer listens and CRM smoke still passes. The user removed old/non-CRM Volcengine security-group rules for TCP `8088`, TCP `8443`, and TCP `3389`; API post-cleanup verification confirms only public TCP `22`, `80`, and `443` remain. | Infrastructure Ops + Security Compliance | G11 handoff to G12; TASK-039 | ACC-017 (P0), ARCH-ACC-013, ARCH-ACC-014, TEST-DEPLOY-SMOKE-001/002 | Resolved | 2026-06-03 |

## Carried-forward Release Blockers (not gate blockers)

Recorded in `planning/gate-status.md` G12 row; repeated here for visibility. These were
release-time evidence items, not modeling blockers: encrypted off-server backup copy +
restore rehearsal, HTTPS/TLS endpoint, security-group, and monitoring evidence. As of
2026-06-03, Codex has recorded runtime evidence for these items under TASK-039 and
TASK-040; G12 still performs independent audit before any release decision. Refs:
OQ-001, RISK-002, `docs/architecture/deployment-notes.md`.

## Go-Live Execution Blockers

These are production go-live execution blockers surfaced after G12 PASS. They block the
release runbook until resolved, but do not change the audited application design or code.

| ID | Blocker | Owner | Blocks | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-GOLIVE-001 | Production go-live could not proceed because `crm-deploy@118.196.44.193` SSH failed before the pre-flight checks could run. TCP 22 was reachable, but SSH closed during key exchange before authentication. Root cause was local VPN/CDN routing plus stale local `known_hosts`: `118.196.44.193` was routed to local gateway `192.168.0.1`, Volcengine console/CDN IPs were also routed direct, the server-side ED25519 host key was confirmed through Volcengine console as `SHA256:I7oOPvVmtattsy08LSSnnfBcJBMh5JqIHRDuEDyZW/8`, and the stale local known_hosts entries were removed. `crm-deploy` SSH then succeeded with strict host-key checking. No deployment step, checkout, backup, migration, compose change, or rollback action was executed while this blocker was open. | Infrastructure Ops | Production go-live; ACC-017 evidence capture | ACC-017 (P0), deploy/ops/go-live-runbook.md | Resolved | 2026-06-05 |
| BLK-GOLIVE-002 | Production go-live could not proceed because the pre-flight secret check could not be completed by the required `crm-deploy` account. Server file `/opt/crm-system/current/.env` existed but was owned `root:root` with mode `600`; `crm-deploy` had only group `crm-deploy` and `test -r .env` returned `no`. The user authorized continuing; Codex used the registered root SSH key only to change the existing production `.env` to least-privilege readable by the deploy group (`root:crm-deploy 640`). `crm-deploy` then verified `.env` exists and all required key names are present without printing values. No checkout, backup, migration, compose change, or rollback action was executed while this blocker was open. | Infrastructure Ops | Production go-live; ACC-017 evidence capture | ACC-017 (P0), deploy/ops/go-live-runbook.md | Resolved | 2026-06-05 |
| BLK-GOLIVE-003 | Production go-live could not proceed because the second pre-flight requirement failed: `/opt/crm-system/current` was not a Git repository, so the server could not run `git fetch --all`, verify/fetch release commit `da9d63c`, or perform the runbook's `git checkout da9d63c`. Evidence: `git fetch --all --prune` from `/opt/crm-system/current` returned `fatal: not a git repository`; `ls -la /opt/crm-system/current` showed application files but no `.git`, with most files owned by stale UID/GID `501:staff`. The user authorized continuing; Codex created a server-local bare origin from a verified local Git bundle, rebuilt `/opt/crm-system/current` as a Git working tree checked out at `da9d63c`, preserved server-only `.env`, and retained the previous copy under `/opt/crm-system/releases/current-pre-golive-20260604T165758Z`. `crm-deploy` verified `git fetch --all --prune`, `git cat-file -e da9d63c^{commit}`, and `git log --oneline -1 da9d63c` successfully. No backup, migration, compose change, or rollback action was executed while this blocker was open. | Infrastructure Ops | Production go-live; ACC-017 evidence capture | ACC-017 (P0), deploy/ops/go-live-runbook.md | Resolved | 2026-06-05 |
| BLK-GOLIVE-004 | **Resolved — independently re-verified by Claude 2026-06-05** (`archive/reviews/g12-audit/post-deploy-reverify-2026-06-05.md`): live probes (HTTPS 200, HTTP→301, 8080/5432 unreachable) plus line-by-line transcript cross-check confirmed all 10 images `created=2026-06-05` with new sha256 IDs, real per-service `go build`, the new migrations applying, containers recreated 12:14, and the dispositive BLK-G12-015 behavioral proof (non-owner Sales by-id quote read → safe 404) — so the audited `da9d63c` build is provably live. LOW follow-up: the running tag `prod-20260603` is misleading (content is the 06-05 rebuild); set `CRM_IMAGE_TAG` to the real release on next deploy. Codex executed `delivery/go-live-rework-1.md`: confirmed the initial image state, took a fresh encrypted/off-site backup (`crm-postgres-20260605T041343Z.sql.gz.enc`, checksum `6733f9e570882a8412f3db52584fcd01dca7e7422f7d207b6b0d5cb4af0a48a2`), rebuilt the frontend, ran `docker compose -f docker-compose.prod.yml up -d --build`, ran `bash scripts/migrate.sh up` with new migration evidence (`account/0006_lead_conversion_idempotency`, `opportunity/0004_lead_conversion_idempotency`, `reporting/0003_reporting_outbox`), and reloaded Nginx. Final evidence shows 10 CRM Go service images have `2026-06-05` image Created dates and new image IDs, all 10 service containers were recreated at `2026-06-05 12:14:00 +0800`, and final `docker compose ps` shows all 11 production containers healthy with gateway still bound to `127.0.0.1:8080`. Behavior proof for BLK-G12-015 captured a non-owner Sales by-id quote read returning safe `HTTP/1.1 404 Not Found`, then deleted the temporary quote and proved `CLEANUP_COUNT=0`. `deploy/healthcheck/check_endpoint.sh` passed; `crm-certbot-renew.timer` is active/enabled and `certbot renew --dry-run` succeeded for the certificate expiring `2026-06-09`. Evidence: `docs/release/evidence/go-live-rework-1-2026-06-05-transcript.txt`. | Infrastructure Ops + Integration Owner | Production go-live; ACC-017 (P0) release validity; the entire G12 audit's value | ACC-017 (P0); BLK-G12-015..030 (must actually be in prod); deploy/ops/go-live-runbook.md | Resolved | 2026-06-05 |

## G12 Audit Findings — Rework before release (2026-06-03)

Surfaced by the first G12 independent audit (`archive/reviews/g12-audit/g12-audit-decision-2026-06-03.md`),
five parallel author≠auditor passes; two dimensions FAILED; first rework package
`delivery/G12-rework.md`. Codex remediated and returned. Claude then ran a focused
multi-agent **G12 RE-AUDIT** (`archive/reviews/g12-audit/g12-reaudit-2026-06-03.md`),
including an **independent compile/test run** (11/11 Go modules build/vet/test green
against real PostgreSQL testcontainers; frontend tsc+build green) and a **Claude-run
read-only live Volcengine query**. Re-audit result at kickback time: **6 of 8 findings
genuinely closed; BLK-G12-001 and BLK-G12-006 remained Open (partially fixed)**,
with new BLK-G12-009/010 added. Second rework package: `delivery/G12-rework-2.md`.
Codex second rework resolved BLK-G12-001/006/009/010. Claude's FINAL focused re-audit
(2026-06-03, `archive/reviews/g12-audit/g12-reaudit-final-2026-06-03.md`; 4 parallel passes +
independent compile/test run: 11/11 Go modules green, 203 tests, 0 skips, real Postgres)
**confirmed all four genuinely fixed** and surfaced ONE new consistency defect, BLK-G12-011
(`lead` non-transactional outbox — same class as BLK-G12-004). Per user decision this is
elevated to a required **3rd kickback** (`delivery/G12-rework-3.md`) before G12 passes;
G11/G12 remain Gate Blocked. No-downgrade applies.

| ID | Severity | Blocker | Owner | Touches | Status (re-audit) | Opened |
|---|---|---|---|---|---|---|
| BLK-G12-001 | BLOCKER | `opportunity/commercial/work/account` wrote `EVT-*` events only to local `outbox_events` with no relay to audit-history. **Second rework fixed** — account, commercial, and work now each have real PostgreSQL testcontainer dispatcher coverage for successful S2S audit-history delivery, failed-delivery retry retention, and duplicate event UID idempotency; opportunity already had the same coverage. | backend-engineer | ACC-014, ACC-022 (P0); AUD-IMM-002 | Resolved | 2026-06-03 |
| BLK-G12-002 | BLOCKER | `reporting` `POST /internal/projections` had no S2S verification. **Re-audit: FIXED** — signed-token middleware enforced (aud/intent/≤5min, fail-closed); negative+positive tests present. | backend-engineer | SEC-SVC-BLK-002 | Resolved | 2026-06-03 |
| BLK-G12-003 | BLOCKER | Security-group cleanup was unsubstantiated and contradicted the only raw API export. **Re-audit: RESOLVED via independent live verification** — Claude ran a read-only Volcengine query (RequestIds `2026-06-03T22:08+08:00`) confirming the CRM ENI is bound only to dedicated SG `sg-366ptx1bxp9ts1e710babmc8y` (public TCP 22/80/443 only; no 8080/5432; no 8088/8443/3389). NOTE: the earlier `...dedicated-raw-...json` (20:29) recorded only Describe calls (no mutating RequestIds) and is NOT acceptable provenance; the END STATE is nonetheless independently confirmed correct. | infrastructure-ops + security-compliance | ACC-017 (P0); ARCH-ACC-013/014 | Resolved | 2026-06-03 |
| BLK-G12-004 | MAJOR | Outbox append not in the mutation's transaction and error discarded. **Re-audit: FIXED** — single-transaction `inTransaction` helper across all four services; append error returned (rollback + 503); testcontainer rollback test `TEST-HISTORY-TX-001`. | backend-engineer | AUD-IMM-002 | Resolved | 2026-06-03 |
| BLK-G12-005 | MAJOR | `reporting` projection ingest never invoked; authz over empty data. **Re-audit: FIXED** — four source domains deliver to reporting via signed dispatchers; consumer upserts read-model; end-to-end + authz tests present. | backend-engineer | PM-043..045 (P1) | Resolved | 2026-06-03 |
| BLK-G12-006 | MAJOR | Release evidence not independently verifiable. **Second rework fixed** — restore counts now match captured catalog output (roles/schemas/servicePermissionRoles all `10`), `crm-deploy` and `crm-ops` use distinct Ed25519 key fingerprints, `sshd -T` proves password auth is disabled and keyboard-interactive is disabled, and `8080`/`5432` negative probes were rerun from external edge `srv-aliyun-bj-01`. | infrastructure-ops | ACC-017 (P0); operator-access review conditions | Resolved | 2026-06-03 |
| BLK-G12-007 | MAJOR | CRM shared the `Default` security group. **Re-audit: RESOLVED via live verification** — CRM ENI bound only to dedicated SG `sg-366ptx...`; Default SG no longer governs the CRM instance. Same evidence as BLK-G12-003. | infrastructure-ops | ACC-017; deployment-notes Network Exposure | Resolved | 2026-06-03 |
| BLK-G12-008 | MINOR | Test-traceability + hardening gaps. **Re-audit: FIXED** — 7 P0 tests now tagged 1:1; `work` S2S 5-min cap enforced; `AUTHZ_VERSION_STALE` enforced; root screenshot removed; all regression checks PASS. | backend-engineer | traceability; minor security hardening | Resolved | 2026-06-03 |
| BLK-G12-009 | MAJOR | Evidence-integrity gap: `scripts/test_security_group_evidence.py` validated only JSON shape and accepted a read-only snapshot as "success". **Second rework fixed** — `--apply` remediation evidence now requires `CreateSecurityGroup`, `AuthorizeSecurityGroupIngress`, `RevokeSecurityGroupIngress`, and `ModifyNetworkInterfaceAttributes` RequestIds with HTTP 200; read-only exports are accepted only with `--verification`. | infrastructure-ops | evidence integrity | Resolved | 2026-06-03 |
| BLK-G12-010 | MINOR | Reporting projection delivery omitted `X-Correlation-Id`, and no Administrator all-scope report test existed. **Second rework fixed** — lead/account/opportunity/commercial projection dispatchers now propagate `X-Correlation-Id`; opportunity dispatcher test proves the header; reporting now has `TEST-BASIC-REPORT-006` for Administrator all-scope reports. | backend-engineer | STB-003; PM-043 | Resolved | 2026-06-03 |
| BLK-G12-011 | MAJOR | `lead` service uses a NON-transactional fire-and-forget outbox: `_ = h.outbox.Append(...)` is called after `repo.Create` commits and the error is discarded (`lead_command.go:115/153/207`, `lead_qualify.go:78`, `lead_convert.go:85`, `duplicate_check.go:43`). This is the SAME class of defect that BLK-G12-004 required fixed for opportunity/commercial/account/work; lead was cited as the "durable reference" but is not transactional. If the Append fails, the lead mutation commits with no outbox row, so `EVT-LEAD-QUALIFIED/DISQUALIFIED/CONVERTED` and lead `EVT-OWNER-CHANGED` can be permanently lost (no row to retry). Pre-existing (TASK-032), surfaced by the final G12 re-audit; previously marked Optional in rework #2 — now elevated to a required 3rd-kickback fix per user decision (consistency + no-downgrade). **Third rework fixed + verified** — lead create/owner-transfer/qualification/conversion/archive/duplicate-warning writes now append outbox rows in the same DB transaction and return dependency failure on append error; fail-first rollback test `TEST-HISTORY-TX-001` (lead) green; independent build/test 11/11 green; spot re-audit (3 parallel passes) confirmed FIXED. | backend-engineer | ACC-014, ACC-022 (P0); AUD-IMM-002 | Resolved | 2026-06-03 |

### G12 spot re-audit minor follow-ups (2026-06-04) — non-blocking, 4th micro-rework

Surfaced by the BLK-G12-011 spot re-audit (`archive/reviews/g12-audit/g12-reaudit-final-2026-06-03.md`). None blocks release; the release owner elected to have Codex fix them before G12 passes. Package: `delivery/G12-rework-4.md`.

| ID | Severity | Blocker | Owner | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-G12-012 | MINOR | `lead` qualify (valid) and disqualify (invalid) emit the SAME outbox event constant `event.LeadQualified` (distinguished only by payload `status`/`invalidReason`), so the distinct catalog event `EVT-LEAD-DISQUALIFIED` never appears as its own event_id in the audit log. Emit a distinct event/type for disqualify mapping to `EVT-LEAD-DISQUALIFIED`. **Fourth micro-rework fixed** — invalid qualification now emits `LeadDisqualified`, and dispatcher mapping resolves it to `EVT-LEAD-DISQUALIFIED`; valid remains `EVT-LEAD-QUALIFIED`. | backend-engineer | ACC-014; audit-log-spec EVT-LEAD-DISQUALIFIED | Resolved | 2026-06-04 |
| BLK-G12-013 | MINOR | `lead` qualify path makes an additional NON-transactional post-commit `audit.AppendRecordHistory` call (502 on failure) separate from the now-transactional outbox. Codex must determine whether lead's audit-history delivery is via the transactional outbox dispatcher (preferred) and, if so, REMOVE the redundant post-commit non-transactional call; if the post-commit call is in fact lead's only audit-history path, route it through the transactional outbox instead (so no post-commit non-transactional audit window remains). **Fourth micro-rework fixed** — lead outbox dispatcher now delivers audit-history events with retry retention; the post-commit `audit.AppendRecordHistory` call was removed from qualification. | backend-engineer | ACC-014, ACC-022 (P0); AUD-IMM-002 | Resolved | 2026-06-04 |
| BLK-G12-014 | MINOR | No dedicated lead integration test asserts end-to-end outbox→reporting delivery (the transactional enqueue and reporting ingest are each tested, but the lead-side dispatcher poll/POST path is not). Add a delivery test modeled on `services/opportunity/internal/event/dispatcher_test.go`. **Fourth micro-rework fixed** — lead dispatcher test uses real PostgreSQL testcontainers and asserts S2S reporting headers, `X-Correlation-Id`, and failed-delivery retry retention. | backend-engineer | PM-043..045 (P1); test coverage | Resolved | 2026-06-04 |

> **BLK-G11-002 RE-RESOLVED (2026-06-03):** the original "Resolved" claim rested on a
> hand-authored file and was correctly reopened by BLK-G12-003. It is now genuinely
> resolved together with BLK-G12-003/007 by Claude's independent read-only live API
> verification (`docs/release/evidence/volcengine-security-group-verified-readonly-2026-06-03.json`),
> not by the contradicted/snapshot-only earlier files.

## G12 Whole-Repo Systematic Sweep Findings (2026-06-04) — REWORK #5

Found by a 7-dimension whole-repo systematic sweep (`archive/reviews/g12-audit/g12-systematic-sweep-2026-06-04.md`)
that audited code never touched by rounds 1–4 (which only audited Codex's changes). These are
release-blocking; the narrow prior rounds missed them. Rework package: `delivery/G12-rework-5.md`.
Codex constraints: **no downgrade AND no over-reach** (fix only these; no scope/model change; no
unilateral shared-lib refactor; raise decisions instead of deciding). No-downgrade applies.

| ID | Severity | Blocker | Owner | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-G12-015 | BLOCKER | commercial single-record read IDOR: `getContract` (`contract_query.go:25`) and `getQuote` (`quote_query.go:24`) return records by id with no ownership/CanRead check; any Sales user can read any contract/quote by id enumeration. List endpoints are scoped; by-id read is not; commercial domain has no CanRead. **Fixed** — by-id quote/contract reads now enforce the same owner-scope rule as commercial mutations/lists; denied Sales reads return safe `NOT_FOUND` with no record data. | backend-engineer | PM-020/021 (P0) | Resolved | 2026-06-04 |
| BLK-G12-016 | BLOCKER | import-export `getImportRun` IDOR (`import_run.go:169`): no actor/ownership check; any authenticated user reads any import run by id, exposing filename + per-row PII. **Fixed** — `getImportRun` now requires actor headers and enforces import-run owner/team/Admin scope; denied reads return safe `NOT_FOUND` without run id, filename, or row-error details. Evidence: fail-first `go test ./internal/handler -run TestImportRunReadAuthorization -count=1` exposed cross-team/Sales/anonymous 200 leaks; after fix that test and `go test ./... -count=1` pass in `services/import-export`. | backend-engineer | PM-034..036 (P0/P1) | Resolved | 2026-06-04 |
| BLK-G12-017 | MAJOR | identity-authz audit not durable/transactional: login/role/status/last-admin events emitted post-commit, non-transactional, error discarded (`user_admin.go:211`); `identity_authz.outbox_events` written but never dispatched (dead table). Most security-critical audit is least reliable. **Fixed** — sign-in/sign-out and admin create/role/status/last-admin success/blocked paths now write business/session mutation and local outbox event in the same DB transaction; user-admin post-commit `AppendOperationLog` was removed; identity-authz starts an outbox dispatcher that sends signed S2S audit-history appends and keeps failed rows unpublished for retry. Evidence: fail-first `go test ./internal/handler -run TestUserAdminAuditOutboxFailureRollsBackMutation -count=1` first returned 201 and persisted a user after forced outbox failure; fail-first `go test ./internal/event -run TestOutboxDispatcherDeliversAuditHistoryAndRetries -count=1` first failed to compile because there was no dispatcher; after fix both pass and `go test ./... -count=1` passes in `services/identity-authz`. | backend-engineer | ACC-022 (P0); AUD-IMM-002 | Resolved | 2026-06-04 |
| BLK-G12-018 | MAJOR | work `changeTaskStatus` (`work_command.go:240`, ACC-012 P0) has no optimistic concurrency: doesn't read `expectedVersion`; `ErrVersionConflict` funneled to `writeOutboxFailure` not `409 VERSION_CONFLICT`; stale concurrent edits silently lost. **Fixed** — task status changes now require positive `expectedVersion`, pass it to the repo optimistic-lock update, and map stale updates to `409 VERSION_CONFLICT` without overwriting the concurrent edit. Evidence: fail-first `go test ./internal/handler -run TestWorkAcceptance -count=1` first returned 200 and changed a version-2 task to Completed when called with stale `expectedVersion:1`; after fix that test and `go test ./... -count=1` pass in `services/work`. | backend-engineer | ACC-012 (P0) | Resolved | 2026-06-04 |
| BLK-G12-019 | MAJOR | lead conversion saga lacks downstream idempotency: account/opportunity creates don't honor an idempotency key and the lead-side key is persisted only after both succeed; retry after partial success creates an orphaned duplicate account. **Fixed** — lead now sends stable per-downstream conversion idempotency keys (`:account`, `:opportunity`); account and opportunity persist nullable lead-conversion idempotency keys in their own schemas and return the original record on repeated internal create calls. Evidence: fail-first lead retry test first made two downstream account creates after opportunity's first failure; account/opportunity fail-first internal create tests first returned 201 twice and inserted duplicates. After fix `go test ./... -count=1` passes in services/lead, services/account, and services/opportunity. | backend-engineer | ACC-004 (P0) | Resolved | 2026-06-04 |
| BLK-G12-020 | MAJOR | 7 synchronous internal clients omit `X-Correlation-Id` (trace break, STB-003): opportunity commercial_client, lead conversion_client, account archive, work work_query, import-export audit_client (×2), lead audit_client, identity-authz audit_client. **Fixed** — synchronous internal clients now propagate `X-Correlation-Id` from the inbound request or audit input; identity-authz audit client is aligned even though user-admin delivery now uses the dispatcher. Evidence: fail-first tests for opportunity close-won commercial check, lead conversion downstream calls, account archive work obligation query, work reminder commercial query, and import-export audit client first failed on missing correlation; after fix those target tests pass. Full `go test ./... -count=1` passed in services/opportunity, lead, account, work, import-export, and identity-authz (work first run had a PostgreSQL testcontainer connection reset during migration, immediate rerun passed). | backend-engineer | STB-003 | Resolved | 2026-06-04 |
| BLK-G12-021 | MAJOR | 8 of 24 EVT catalog events never emitted under catalog id / untested: 6 identity-auth events collapsed to raw strings / `EVT-USER-ADMIN-CHANGED`; `EVT-CONTRACT-TERMINATED` folded into generic; `EVT-REPORT-ACCESS-DENIED` never emitted. Violates audit-log-spec Testability. **Fixed** — identity-authz now maps login succeeded, login failed, access denied, role changed, status changed, and last-admin blocked to the audit-log-spec IDs; commercial terminated contracts map to `EVT-CONTRACT-TERMINATED`; reporting denied report access writes local outbox rows and dispatches signed S2S audit-history appends with retry retention. Evidence: fail-first `go test ./internal/event -run TestIdentityAuditCatalogEventIDs -count=1`, `go test ./internal/event -run TestCommercialAuditCatalogEventIDs -count=1`, `go test ./internal/handler -run TestReportAccessDeniedCreatesCatalogEvent -count=1`, and `go test ./internal/event -run TestReportingAccessDeniedDispatchesAuditCatalogEventAndRetries -count=1` first failed on collapsed/missing events; after fix those tests passed, and `go test ./... -count=1` passed in services/identity-authz, services/commercial, and services/reporting. `EVT-STATUS-CHANGED`, `EVT-TASK-COMPLETED`, `EVT-TASK-CANCELLED`, and `EVT-PAYMENT-OVERDUE` remain outside this fix because PSM/API entries allow status/reminder event folding and G12-rework-5 instructed to preserve those if design-folded. | backend-engineer | ACC-022 (P0); audit-log-spec | Resolved | 2026-06-04 |
| BLK-G12-022 | MAJOR | account + commercial outbox→reporting delivery untested (BLK-G12-014 class): both deliver to reporting in prod but dispatcher tests never set `ReportingServiceURL`. **Fixed** — account and commercial dispatcher integration tests now set `ReportingServiceURL`, assert signed S2S reporting headers, `X-Correlation-Id`, projection payloads, and failed reporting delivery retry retention with real PostgreSQL testcontainers. Evidence: `go test ./internal/event -run TestOutboxDispatcherDeliversReportingProjectionAndRetries -count=1` passed in services/account and services/commercial; `go test ./... -count=1` passed in both services. | backend-engineer | PM-043..045 (P1); test coverage | Resolved | 2026-06-04 |
| BLK-G12-023 | MAJOR | `docs/architecture/api-spec.md:378-392` Close-Won request schema omits the required `contractId` (and lists an unused `idempotencyKey`); a spec-conformant client cannot satisfy DEC-017. Doc fix; code is correct. **Fixed** — Close-Won request now documents required `contractId` and removes the unused `idempotencyKey`; a doc-contract test guards the spec. Evidence: fail-first `python3 scripts/test_api_spec_contract.py` first failed on missing `contractId`; after fix it passes. | backend-engineer | DEC-017; api-spec | Resolved | 2026-06-04 |
| BLK-G12-024 | MINOR | Error/denial uniformity: existing-but-unreadable single-record reads leak existence (403 vs 404 split); identity-authz 2-arg codeless error envelope (`auth.go:253`); audit-history verifier not fail-closed on empty audience/intent; identity-authz verifier uses local time not UTC; `/internal/sessions/check` cookie-only (confirm+document). **Fixed** — account/lead/opportunity non-owned detail reads now return safe `404 NOT_FOUND` with no record data; identity-authz auth failures use explicit `AUTHENTICATION_FAILED` / `authentication` envelope calls; audit-history S2S verification rejects empty audience/intent; identity-authz verifier defaults to UTC; api-spec documents `/internal/sessions/check` as cookie-only. Evidence: fail-first record-read tests first returned `403 PERMISSION_DENIED`; fail-first audit-history token test first accepted empty audience/intent; fail-first denial-contract script first flagged the auth helper, local time, and missing session-check documentation. After fix target tests and `go test ./... -count=1` passed in services/account, lead, opportunity, audit-history, and identity-authz; `python3 scripts/test_denial_contract.py` passed. | backend-engineer | Denial Contract; STB | Resolved | 2026-06-04 |
| BLK-G12-025 | LOW | Cleanup: dead code (lead `audit_client.go`, zero-call helpers `IsServiceAuthFailed`/`HasSurface`/`IsForeignKeyError`/`ProjectionTableName`/`ExportScope`/`EventEnvelope`; `shared/contracts` unconsumed — raise, don't delete unilaterally); opportunity dispatcher test depth (reporting fail-path + dedup); 2 test-model id mis-citations; `.secrets` not in project `.gitignore`. **Fixed** — removed verified zero-call lead post-commit audit client and helper/type definitions while preserving live CSV escape code; retained `shared/contracts/EventEnvelope` because the whole shared contracts module is an explicitly planned/preallocated contract package and G12-rework-5 forbids unilateral deletion; added opportunity reporting failure retry + stable audit UID duplicate assertions; corrected `TEST-NAV-RETRIEVE-006` and `TEST-TASK-LIFECYCLE-004` citations; added `.secrets/` to `.gitignore` and confirmed no tracked `.secrets` paths. Evidence: fail-first `python3 scripts/test_cleanup_contract.py` first failed on dead code and missing `.secrets/`; after fix it passes. `go test ./... -count=1` passed in services/lead, import-export, audit-history, opportunity, account, reporting, and work. | backend-engineer | hygiene | Resolved | 2026-06-04 |

## G12 Full Systematic RE-SWEEP Residual Tail (2026-06-04) — REWORK #6 (consolidated)

Found by the full whole-repo systematic RE-SWEEP verifying rework #5
(`archive/reviews/g12-audit/g12-resweep-2026-06-04.md`, 6 parallel author≠auditor agents). The
11 registered BLK-G12-015..025 were all confirmed genuinely FIXED, with **no over-reach and no
downgrade** and build/vet/frontend green; the re-sweep drained a residual tail of one MAJOR +
three MINOR + one LOW-batch (none release-blocking). Per release-owner decision (2026-06-04) these
are fixed in one consolidated micro-rework before G12 passes. Package: `delivery/G12-rework-6.md`.
Codex constraints: **no downgrade AND no over-reach** (fix only these; no shared-lib refactor; no
scope/model change; raise decisions instead of deciding). No-downgrade applies.

| ID | Severity | Blocker | Owner | Touches | Status | Opened |
|---|---|---|---|---|---|---|
| BLK-G12-026 | MAJOR | identity-authz **denial-path** security audit still non-durable: `auth.go:250` `appendAccessDenied` (EVT-AUTH-LOGIN-FAILED + unauthenticated/invalid-session/inactive/authz-stale/user-admin-denied) and `permission.go:64` (EVT-AUTH-ACCESS-DENIED) write via non-transactional `h.outbox.Append` with the error swallowed by `log.Printf`. **Fixed** — denial audit appends now use identity-authz local outbox transactions, append failures surface `503 DEPENDENCY_UNAVAILABLE`, and permission-denied append errors are no longer swallowed. Evidence: fail-first `TEST-AUDIT-DENIAL-DURABLE-001/002`; `go test ./internal/handler -run 'TestDenialAuditOutboxFailureIsSurfaced|TestPermissionDeniedAuditOutboxFailureIsSurfaced' -count=1`; `go test ./... -count=1` passed in services/identity-authz. | backend-engineer | ACC-022 (P0); AUD-IMM-002 | Resolved | 2026-06-04 |
| BLK-G12-027 | MINOR | Dead code `identity-authz/internal/authz/audit_client.go` (whole file; `NewAuditClient`/`AuditClient`/`AppendOperationLog`) has zero call sites after 017 moved identity audit to the dispatcher, and still hard-codes the stale collapsed id `EVT-USER-ADMIN-CHANGED` that BLK-G12-021 replaced. **Fixed** — `rg` verified no production call sites, the file was removed, and `scripts/test_cleanup_contract.py` now asserts absence. Evidence: `rg -n 'EVT-USER-ADMIN-CHANGED|NewAuditClient|AppendOperationLog|type AuditClient' services/identity-authz scripts` returns no matches; `python3 scripts/test_cleanup_contract.py` passed. | backend-engineer | hygiene; audit-log-spec | Resolved | 2026-06-04 |
| BLK-G12-028 | MINOR | 8 catalog events have an emission path + handler/outbox test but **no test pins the derived catalog `eventId`**: EVT-LEAD-CONVERTED, EVT-QUOTE-ACCEPTED, EVT-PAYMENT-RECORDED, EVT-OPPORTUNITY-WON, EVT-OPPORTUNITY-LOST, EVT-RECORD-ARCHIVED, EVT-IMPORT-RUN, EVT-EXPORT-RUN. **Fixed** — added eventId assertions in lead, commercial, opportunity, account, and import-export tests. Evidence: `TEST-EVT-CATALOG-LEAD-001`, `TEST-EVT-CATALOG-COMMERCIAL-002..004`, `TEST-EVT-CATALOG-OPPORTUNITY-001..003`, `TEST-EVT-CATALOG-ACCOUNT-001`, `TEST-EVT-CATALOG-IMPORTEXPORT-001`; `go test ./... -count=1` passed in services/lead, commercial, opportunity, account, and import-export. | backend-engineer | audit-log-spec Testability | Resolved | 2026-06-04 |
| BLK-G12-029 | MINOR | Denial existence-leak residual: `account listContactsForAccount` returns `403 PERMISSION_DENIED` for an existing-but-unreadable account, while `getAccount` returns `404` — leaks which account ids exist via the contacts sub-resource. **Fixed** — unreadable account contacts now return safe `404 NOT_FOUND` with no contact data. Evidence: fail-first `TEST-DENIAL-CONTACTS-001`; `go test ./internal/handler -run 'TestContactLinkAcceptance|TestAccountLeadConversionCreateIdempotency|TestAccountDuplicateWarningsAcceptance' -count=1`; `go test ./... -count=1` passed in services/account. | backend-engineer | Denial Contract; STB | Resolved | 2026-06-04 |
| BLK-G12-030 | LOW | Consistency/robustness batch (in-place, no shared lib): (a) work S2S verifier naming/error shape; (b) concurrent lead-conversion duplicate-key mapping; (c) account duplicate-warning two-transaction window. **Fixed** — work verifier now uses `ServiceTokenClaims`/`ErrServiceAuthFailed` in place; account/opportunity internal lead-conversion unique-key races return `200` existing row; account duplicate warning token and `DuplicateWarningRaised` outbox append now share one DB transaction. Evidence: `TEST-SVC-TOKEN-LIFETIME-001`, `TEST-LEAD-CONVERSION-IDEMPOTENCY-004/005`, `TEST-DUPLICATE-WARN-TX-001`; `go test ./... -count=1` passed in services/work, account, and opportunity. | backend-engineer | hygiene; consistency | Resolved | 2026-06-04 |

## Resolution Log

| Date | ID | Resolution | Deciding source |
|---|---|---|---|
| 2026-06-01 | BLK-001 | Opportunity `Status` field removed; Pipeline Stage is the sole lifecycle dimension. The "what are Status's values" gap is dissolved, not answered. | DEC-020 (Formal Scope Change by User) |
| 2026-06-01 | BLK-002 | Won decoupled from full payment (Won = contract Signed); multi-plan "fully paid" is no longer a Won precondition, so the aggregation gate is moot. Overpayment ceiling remains contract-level. | DEC-017 + DEC-019 (Formal Scope Change by User) |
| 2026-06-01 | BLK-003 | Each opportunity has exactly one quote; there is no second quote to accept, so the reject-vs-auto-demote question no longer exists. | DEC-018 (Formal Scope Change by User) |
| 2026-06-01 | BLK-A01 | Overdue-evaluation trigger resolved at PSM as on-read evaluation against the supplied `businessDate` (Asia/Shanghai); deterministic for G7 test design. | PSM "Resolved Mechanisms"; api-spec.md Reminder Query; FLOW-005 |
| 2026-06-03 | BLK-G11-001 | The user approved `https://118.196.44.193` as the production HTTPS endpoint. Codex installed Certbot 5.6.0, issued a Let's Encrypt IP certificate with SAN `IP Address:118.196.44.193`, enabled Nginx 443, verified HTTP→HTTPS redirect and server-side deploy smoke, and configured renewal timer/dry-run. | `docs/release/acc-017-evidence-template.md`; TASK-039 server evidence |
| 2026-06-03 | BLK-G11-002 | Volcengine API evidence confirms CRM `8080` and PostgreSQL `5432` are not publicly allowed; host-network Hermes `8642` was stopped/removed; old/non-CRM security-group rules TCP `8088`, TCP `8443`, and TCP `3389` were removed by the user and verified by API. | `docs/release/evidence/volcengine-security-group-post-cleanup-2026-06-03.json`; `docs/release/evidence/old-deployment-release-2026-06-03.json`; TASK-039 closure evidence |
| 2026-06-03 | TASK-040 release evidence | Encrypted PostgreSQL backup `crm-postgres-20260603T104620Z.sql.gz.enc` was produced on `srv-volcengine-sh-01`, copied to `srv-aliyun-bj-01`, verified by checksum, and restored in rehearsal run `20260603T104837Z`. `crm-backup.timer` is enabled and active for daily 02:00 backups. | `docs/release/acc-017-backup-evidence-template.md`; `docs/release/evidence/backup-restore-rehearsal-2026-06-03.json`; TASK-040 closure evidence |
| 2026-06-03 | BLK-G12-001 | Implemented outbox dispatchers for opportunity, account, commercial, and work services. Dispatchers read unpublished local `outbox_events`, send S2S-signed `audit.append` calls to audit-history with the outbox row ID as `eventUid`, leave failed deliveries unpublished for retry, and mark success with `published_at`. audit-history accepts producer event UID and is idempotent on duplicate UID. Compose/prod compose now configure `AUDIT_HISTORY_SERVICE_URL` for the four producers. | `services/*/internal/event/outbox.go`; `services/*/cmd/server/main.go`; `services/opportunity/internal/event/dispatcher_test.go`; `services/audit-history/internal/handler/append_test.go`; `go test ./...` in opportunity/account/commercial/work/audit-history |
| 2026-06-03 | BLK-G12-004 | Moved append-bearing mutations in opportunity, account, commercial, and work onto one local DB transaction with the outbox insert; append errors are no longer discarded. Added fail-first rollback coverage for opportunity create (`TEST-HISTORY-TX-001`) and removed dropped outbox append patterns from the four audited service handlers. | `services/opportunity/internal/handler/opportunity_command_test.go`; `services/{opportunity,account,commercial,work}/internal/handler`; `services/{opportunity,account,commercial,work}/internal/repo`; `go test ./...` in opportunity/account/commercial/work |
| 2026-06-03 | BLK-G12-002 | Added signed S2S verification to reporting `POST /internal/projections` with audience `reporting`, intent `reporting.projection_ingest`, max 5-minute token lifetime, and `SERVICE_AUTH_FAILED` rejection for missing/expired/wrong-audience/wrong-intent/wrong-signature calls. Negative cases assert no projection mutation; valid token succeeds. | `services/reporting/internal/authz/service_token.go`; `services/reporting/internal/handler/overview_query.go`; `services/reporting/internal/handler/overview_query_test.go`; `go test ./internal/handler -run TestProjectionIngestRequiresS2SToken`; `go test ./...` in services/reporting |
| 2026-06-03 | BLK-G12-005 | Wired source domain outbox events into reporting projection ingest. lead dispatches local outbox events to reporting; account/opportunity/commercial dispatchers now deliver reporting projections before marking an event published. reporting S2S projection ingest populates `record_projections`, Manager report queries read the aggregate, and Sales remains denied. | `services/lead/internal/event/outbox.go`; `services/{account,opportunity,commercial}/internal/event/outbox.go`; `services/reporting/internal/handler/overview_query_test.go`; `docker-compose.yml`; `docker-compose.prod.yml`; `go test ./...` in lead/account/opportunity/commercial/reporting |
| 2026-06-03 | BLK-G12-003 / BLK-G12-007 rework attempt blocked | Added fail-first evidence validation (`TEST-DEPLOY-SG-001`) requiring raw Volcengine API proof that CRM is bound to a dedicated non-Default security group with public TCP ingress limited to 22/80/443 and no public 8080/5432/8088/8443/3389. OpenAPI access succeeded, but remediation stopped at `CreateSecurityGroup` because the current operator account lacks `vpc:CreateSecurityGroup`; G11/G12 remain Gate Blocked until the required VPC IAM actions are granted and the script is rerun. | `scripts/test_security_group_evidence.py`; `scripts/volcengine_security_group_rework.py`; `docs/release/evidence/volcengine-security-group-rework-blocked-2026-06-03.md` |
| 2026-06-03 | BLK-G12-003 / BLK-G12-007 | After VPC IAM permission was granted, reran the OpenAPI remediation. CRM ENI `eni-13e8tbocd8f0g79iu5jer8idt` is now bound only to dedicated non-Default security group `sg-366ptx1bxp9ts1e710babmc8y` (`crm-system-prod-public`). The dedicated group's final raw ingress allows public TCP `22`, `80`, and `443` only. The final raw default-group export has no public TCP `8088`, `8443`, or `3389`, and the final evidence has no public TCP `8080` or `5432`. `TEST-DEPLOY-SG-001` passed. | `docs/release/evidence/volcengine-security-group-dedicated-raw-2026-06-03.json`; `docs/release/evidence/volcengine-security-group-rework-transcript-2026-06-03.txt`; `scripts/test_security_group_evidence.py` |
| 2026-06-03 | BLK-G12-006 | Second rework closed the residual evidence gaps: restore JSON and transcript now match the captured catalog counts (`roles=10`, `schemas=10`, `servicePermissionRoles=10`); `crm-deploy` uses `SHA256:ZGLqXBHGgqy29ZUMFysRjaw579Z3yx1980pIFWBb/b4` and `crm-ops` uses `SHA256:PHl9ZXjKKPzI5oiWrll9Jj60X04+5S7/TMpV1q3AYQA`; `sshd -T` shows `passwordauthentication no`, `kbdinteractiveauthentication no`, and root key-only login; `8080`/`5432` external negative probes ran from `srv-aliyun-bj-01` and timed out as expected. `TEST-RELEASE-EVIDENCE-001` passed. | `docs/release/evidence/external-negative-probes-2026-06-03.txt`; `docs/release/evidence/backup-restore-rehearsal-2026-06-03.json`; `docs/release/evidence/backup-restore-transcript-2026-06-03.txt`; `docs/release/evidence/operator-access-transcript-2026-06-03.txt`; `scripts/test_release_evidence_transcripts.sh` |
| 2026-06-03 | BLK-G12-009 | Hardened `scripts/test_security_group_evidence.py`: `--verification` accepts read-only Describe exports only as state verification, while `--apply` requires mutating API provenance with HTTP 200 RequestIds for `CreateSecurityGroup`, `AuthorizeSecurityGroupIngress`, `RevokeSecurityGroupIngress`, and `ModifyNetworkInterfaceAttributes`. Fail-first test proved the old 20:29 read-only file fails as `--apply`; Claude's read-only verification file passes as `--verification`; an apply fixture with all mutating RequestIds passes. | `scripts/test_security_group_evidence.py`; `scripts/test_security_group_evidence_checker.sh`; `python3 scripts/test_security_group_evidence.py --evidence docs/release/evidence/volcengine-security-group-dedicated-raw-2026-06-03.json --apply` fails as expected; `bash scripts/test_security_group_evidence_checker.sh` |
| 2026-06-03 | BLK-G12-010 | Added `X-Correlation-Id` propagation to reporting projection delivery in lead, account, opportunity, and commercial outbox dispatchers. Added fail-first `TEST-REPORTING-CORRELATION-001` in the opportunity dispatcher test; it initially failed with an empty reporting correlation header, then passed. Added `TEST-BASIC-REPORT-006` proving Administrator all-scope sales report queries include active records across teams. | `services/{lead,account,opportunity,commercial}/internal/event/outbox.go`; `services/opportunity/internal/event/dispatcher_test.go`; `services/reporting/internal/handler/report_query_test.go`; `go test ./...` in lead/account/opportunity/commercial/reporting |
| 2026-06-03 | BLK-G12-008 | Added explicit P0 test IDs to existing permission, denied-mutation, owner-transfer, and open-work cascade tests; added fail-first `TEST-SVC-TOKEN-LIFETIME-001` for work S2S token lifetime and enforced a 5-minute cap; added fail-first `AUTHZ_VERSION_STALE` coverage and now returns that error when session authz version is stale; removed the stray root screenshot. | `services/identity-authz/internal/handler/{auth.go,auth_test.go,permission_test.go,user_admin.go}`; `services/work/internal/authz/service_token.go`; `services/work/internal/authz/service_token_test.go`; `services/{lead,opportunity,work}/internal/handler/*test.go`; `go test ./...` in identity-authz/lead/work/opportunity |
| 2026-06-03 | BLK-G12-003 / BLK-G12-007 (RE-AUDIT, supersedes the two rows above) | Claude G12 re-audit found the Codex `...dedicated-raw-...json` (20:29) recorded only Describe calls — no `CreateSecurityGroup`/`Authorize`/`Revoke`/`ModifyNetworkInterfaceAttributes` RequestIds — so it cannot serve as remediation provenance. Claude then ran an INDEPENDENT read-only live Volcengine query (Describe only) and confirmed the END STATE is genuinely correct: ENI `eni-13e8...` bound only to dedicated SG `sg-366ptx...` (`crm-system-prod-public`), public TCP `22/80/443` only, no public `8080/5432/8088/8443/3389`. Real API RequestIds `2026-06-03T22:08+08:00`, http=200. Underlying remediation accepted on the strength of this independent verification; provenance-capture and checker hardening tracked as BLK-G12-009. | `docs/release/evidence/volcengine-security-group-verified-readonly-2026-06-03.json` (Claude independent live read) |
| 2026-06-03 | BLK-G12-001 (second rework) | Added account, commercial, and work dispatcher integration tests mirroring opportunity: each uses real PostgreSQL testcontainers, appends a local outbox event, verifies `DispatchOnce` sends an S2S-signed audit-history append with stable `eventUid` and correlation ID, leaves failed deliveries unpublished for retry, and preserves the same event UID across duplicate delivery attempts. Fixed `work.outbox_events` migration to include `published_at`, matching the dispatcher contract. | `services/{account,commercial,work}/internal/event/dispatcher_test.go`; `services/work/migrations/0002_work.up.sql`; `go test ./internal/event -run TestOutboxDispatcherDeliversRetriesAndDedupesAuditHistoryEvents` in account/commercial/work; `go test ./...` in account/commercial/work |
| 2026-06-03 | BLK-G12-011 | Moved lead service append-bearing mutations onto one local DB transaction with the outbox insert. `LeadRepo`, `DuplicateRepo`, and lead `Outbox` now support Tx execution; create, owner-transfer, qualification, conversion, archive, and duplicate warning paths no longer discard append errors. Added fail-first `TEST-HISTORY-TX-001`: a trigger forces `lead.outbox_events` insert failure, the request fails, and no lead row remains. | `services/lead/internal/{handler,repo,event}`; `services/lead/internal/handler/lead_command_test.go`; `go test ./internal/handler -run 'TestLeadCreateAssignAndConcurrencyAcceptance/TEST-HISTORY-TX-001' -count=1`; `go test ./... -count=1` in services/lead |
| 2026-06-04 | BLK-G12-012 | Added distinct lead disqualification event support. `qualify-invalid` now appends `LeadDisqualified`, while `qualify-valid` keeps `LeadQualified`; lead dispatcher maps them to `EVT-LEAD-DISQUALIFIED` and `EVT-LEAD-QUALIFIED` respectively, with invalid reason preserved in `afterSummary`. | `services/lead/internal/handler/lead_qualify.go`; `services/lead/internal/event/outbox.go`; `services/lead/internal/handler/qualification_test.go`; `services/lead/internal/event/dispatcher_test.go`; `go test ./internal/handler -run 'TestLeadQualificationAndConversionAcceptance/TEST-LEAD-QUALIFY-002' -count=1`; `go test ./internal/event -run TestLeadOutboxDispatcherMapsQualificationAuditEvents -count=1` |
| 2026-06-04 | BLK-G12-013 | Determined that lead outbox previously dispatched only to reporting, so the post-commit `audit.AppendRecordHistory` call was lead qualification's audit-history path. Routed lead audit-history delivery through the transactional outbox dispatcher instead, removed the post-commit call, and proved audit delivery failure leaves the outbox row unpublished for retry. | `services/lead/internal/event/outbox.go`; `services/lead/internal/handler/lead_qualify.go`; `services/lead/cmd/server/main.go`; `services/lead/internal/event/dispatcher_test.go`; `go test ./internal/event -run TestLeadOutboxDispatcherDeliversAuditHistoryAndReportingWithRetry -count=1`; `rg -n '_ = .*Append|AppendRecordHistory|h\\.audit|AuditClient' services/lead/internal/handler services/lead/internal/event services/lead/cmd/server -g'*.go'` returns no matches |
| 2026-06-04 | BLK-G12-014 | Added lead dispatcher end-to-end reporting coverage using real PostgreSQL testcontainers. The test asserts `/internal/projections` receives signed S2S headers and `X-Correlation-Id`, and reporting failure keeps the event unpublished until a successful retry. | `services/lead/internal/event/dispatcher_test.go`; `services/lead/internal/event/outbox.go`; `go test ./internal/event -run TestLeadOutboxDispatcherDeliversAuditHistoryAndReportingWithRetry -count=1`; `go test ./... -count=1` in services/lead |
| 2026-06-04 | BLK-G12-022 | Added account and commercial dispatcher end-to-end reporting coverage using real PostgreSQL testcontainers. The tests assert `/internal/projections` receives signed S2S headers and `X-Correlation-Id`, validate the projection payload, and prove reporting failure leaves the event unpublished until a successful retry. | `services/account/internal/event/dispatcher_test.go`; `services/commercial/internal/event/dispatcher_test.go`; `go test ./internal/event -run TestOutboxDispatcherDeliversReportingProjectionAndRetries -count=1` in services/account and services/commercial; `go test ./... -count=1` in services/account and services/commercial |
| 2026-06-04 | BLK-G12-023 | Aligned the Close-Won API spec with DEC-017 and the implemented opportunity handler: request body now requires `contractId` and no longer documents unused `idempotencyKey`. Added a doc-contract test to prevent regression. | `docs/architecture/api-spec.md`; `scripts/test_api_spec_contract.py`; fail-first `python3 scripts/test_api_spec_contract.py` first failed on missing `contractId`, then passed after the spec fix |
| 2026-06-04 | BLK-G12-024 | Unified denial/error contracts: account/lead/opportunity non-owned detail reads now return safe `NOT_FOUND`; identity-authz auth failures use explicit error code/category; audit-history service-token verification fails closed on empty audience/intent; identity-authz token verifier defaults to UTC; `/internal/sessions/check` is documented as cookie-only. | `services/{account,lead,opportunity}/internal/handler/*query.go`; `services/identity-authz/internal/{handler/auth.go,authz/service_token.go}`; `services/audit-history/internal/authz/service_token.go`; `docs/architecture/api-spec.md`; `scripts/test_denial_contract.py`; `go test ./... -count=1` in account/lead/opportunity/audit-history/identity-authz; `python3 scripts/test_denial_contract.py` |
| 2026-06-04 | BLK-G12-025 | Removed verified dead code, added cleanup guard script, deepened opportunity dispatcher reporting failure/UID-dedup coverage, corrected test ID citations, and added `.secrets/` to `.gitignore`. `shared/contracts/EventEnvelope` was retained by design because the shared contracts module is preplanned and G12-rework-5 forbids unilateral deletion; if product/architecture wants to retire that module, that is a separate scope decision. | `.gitignore`; `scripts/test_cleanup_contract.py`; `services/opportunity/internal/event/dispatcher_test.go`; `services/{account,lead,opportunity}/internal/handler/archive_test.go`; `services/work/internal/handler/work_command_test.go`; deleted `services/lead/internal/client/audit_client.go`; removed zero-call helpers/types; `python3 scripts/test_cleanup_contract.py`; `go test ./... -count=1` in lead/import-export/audit-history/opportunity/account/reporting/work |
| 2026-06-04 | BLK-G12-015 | Added commercial single-record read authorization for quotes and contracts. Sales users can read only records whose `owner_id` matches `X-Actor-User-Id`; Manager/Administrator retain access. Denied by-id reads return safe `NOT_FOUND` and do not leak record IDs or opportunity IDs. | `services/commercial/internal/handler/{quote_query.go,contract_query.go,response.go,commercial_query_test.go}`; fail-first `go test ./internal/handler -run TestCommercialSingleRecordReadAuthorization -count=1` initially returned 200/leaked data; after fix `go test ./internal/handler -run TestCommercialSingleRecordReadAuthorization -count=1` and `go test ./... -count=1` passed in services/commercial |
| 2026-06-03 | BLK-G11-002 (re-resolved) | Re-resolved together with BLK-G12-003/007 by Claude's independent read-only live API verification, replacing the earlier contradicted/snapshot-only files as the deciding source. | `docs/release/evidence/volcengine-security-group-verified-readonly-2026-06-03.json` |
