# G12 Full Whole-Repo Systematic RE-SWEEP — Verify Rework #5 [KICKBACK #6]

## Document Control

- Project: CRM System
- Gate: G12 — Audit → Release/Rework
- Owner: Audit (Claude), independent of execution
- Date: 2026-06-04
- Method: 6 parallel author≠auditor agents. Same whole-repo systematic discipline as the
  2026-06-04 sweep (NOT scoped to recent changes): (1) authz/IDOR by-id sweep across all 10
  services; (2) transactional audit integrity + optimistic concurrency + saga idempotency;
  (3) EVT catalog coverage + correlation-id + cross-service uniformity; (4) dispatcher/delivery
  test coverage + whole-repo test integrity; (5) no-over-reach + scope/docs/cleanup fidelity;
  (6) independent build/vet/test + frontend build.
- Decision: **REWORK #6 (consolidated micro-rework)** — Gate NOT Passed. No release.
  Per release-owner decision (2026-06-04): drain the residual tail fully, then pass.

## What was verified GREEN

**All 11 registered blockers BLK-G12-015..025 are genuinely FIXED** with real cited
artifacts and tests:

- BLK-G12-015 commercial by-id IDOR — FIXED. `contract_query.go:36` / `quote_query.go:35`
  enforce `canReadCommercialRecord`→404; `TestCommercialSingleRecordReadAuthorization` asserts
  non-owner Sales gets 404 with no id/opportunity-id leak.
- BLK-G12-016 import-export getImportRun IDOR — FIXED. `import_run.go:184` `canReadImportRun`;
  `TestImportRunReadAuthorization` proves cross-team/Sales/anonymous → 404, no filename/row leak.
- BLK-G12-017 identity-authz durable audit — **mutation events FIXED** (sign-in/out, create/
  role/status/last-admin now in-transaction outbox + running dispatcher; fail-first
  `TestUserAdminAuditOutboxFailureRollsBackMutation` + `TestOutboxDispatcherDeliversAuditHistoryAndRetries`).
  **PARTIAL on the denial subset — see BLK-G12-026.**
- BLK-G12-018 work optimistic concurrency — FIXED. `work_command.go:245/291` reads
  expectedVersion → 409 VERSION_CONFLICT; `TEST-WORK-VERSION-CONFLICT-001` proves no overwrite.
- BLK-G12-019 lead conversion idempotency — FIXED. migration 0004 + per-downstream keys
  (`:account`/`:opportunity`) + partial unique index; retry returns existing row, no duplicate.
- BLK-G12-020 7 sync clients correlation-id — FIXED. all 7 set X-Correlation-Id with tests.
- BLK-G12-021 8 EVT catalog events — FIXED. all emit under catalog id with positive tests;
  the 4 folded events correctly NOT force-emitted and documented.
- BLK-G12-022 account/commercial→reporting delivery tests — FIXED. both dispatcher tests set
  ReportingServiceURL, assert S2S + X-Correlation-Id + failure-retry on real Postgres.
- BLK-G12-023 api-spec Close-Won contractId — FIXED, doc-only, handler untouched.
- BLK-G12-024 denial/error uniformity — FIXED for the primary by-id reads (403→404), error
  envelope, verifier fail-close, UTC, sessions/check doc. **One sub-resource residual — BLK-G12-029.**
- BLK-G12-025 cleanup — FIXED; `shared/contracts` correctly RAISED not deleted.

**Two hard constraints — both honored:**

- **NO OVER-REACH (NONE).** `shared/` byte-for-byte unchanged in the rework-5 range; no new
  shared S2S/error/outbox library; no new go.mod require; no DEC/model/architecture/boundary
  change; `shared/contracts` preserved and raised as a decision, not deleted; two judgment calls
  (contracts retention, 4 folded EVTs) surfaced rather than self-decided. Traceability spot-check
  6/6 honest.
- **NO DOWNGRADE (NONE).** No mock/stub/TODO; 3 retired tests still absent; 0 skips repo-wide;
  the only two changed test assertions are `403→safe 404` strengthenings, not weakenings.

**Build:** 11/11 Go modules build + vet clean; frontend tsc + build clean. DB integration tests
are wired to real `postgres:16-alpine` and panic-not-skip when Docker is absent (no mock path);
this sweep's sandbox had no Docker daemon, so DB tests were verified by code+wiring inspection,
not re-executed. (Prior rounds independently executed them green: 11/11, 203+ tests, 0 skips.)

## Residual tail found by the re-sweep (none release-blocking; elected for cleanup before pass)

| ID | Severity | Finding |
|---|---|---|
| BLK-G12-026 | MAJOR | identity-authz **denial-path** security audit still non-durable. `auth.go:250` `appendAccessDenied` (EVT-AUTH-LOGIN-FAILED + unauthenticated/invalid-session/inactive/authz-stale/user-admin-denied) and `permission.go:64` (EVT-AUTH-ACCESS-DENIED) write via non-transactional `h.outbox` with the error swallowed by `log.Printf`. BLK-G12-017 made the *mutation* (change) events durable but left the *denial* events best-effort — a failed append on a failed-login/access-denied event is silently lost. Two independent agents flagged this. (Note: AUD-IMM-002's "same durable workflow as the sensitive change" arguably does not bind a denial, since a denial is not a change — so this is partly a new-scope security question, not strictly a miss of 017 as written. Decision: make these durable too, for audit-trail completeness on exactly the events most relevant to intrusion detection.) |
| BLK-G12-027 | MINOR | Dead code: `identity-authz/internal/authz/audit_client.go` (whole file, `NewAuditClient`/`AuditClient`/`AppendOperationLog`) has zero call sites after 017 moved identity audit to the dispatcher, AND still hard-codes the stale collapsed id `EVT-USER-ADMIN-CHANGED` — the exact id BLK-G12-021 set out to eliminate. BLK-G12-025's cleanup missed it. Risk: a future caller could reintroduce the collapsed-id contract. |
| BLK-G12-028 | MINOR | 8 catalog events have an emission path + a handler/outbox test, but **no test pins the derived catalog `eventId`**: EVT-LEAD-CONVERTED, EVT-QUOTE-ACCEPTED, EVT-PAYMENT-RECORDED, EVT-OPPORTUNITY-WON, EVT-OPPORTUNITY-LOST, EVT-RECORD-ARCHIVED, EVT-IMPORT-RUN, EVT-EXPORT-RUN. A mapping regression in `auditEventContract` would go undetected. Contrast EVT-STAGE-CHANGED which IS pinned (`opportunity dispatcher_test.go:135`). audit-log-spec Testability is met loosely but not tightly. |
| BLK-G12-029 | MINOR | Denial existence-leak residual: `account listContactsForAccount` (`account_command.go`) returns **403 PERMISSION_DENIED** for an existing-but-unreadable account, while `getAccount` for the same id returns 404 — an attacker can enumerate which account ids exist via the contacts sub-resource. BLK-G12-024 fixed the primary by-id reads but not this sub-resource. (Only existence, not contact data, leaks.) |
| BLK-G12-030 | LOW | Consistency/robustness batch (no functional defect): (a) `work/internal/authz/service_token.go` uses `ServiceClaims` + `errors.New("invalid claims")` instead of the `ServiceTokenClaims`/`ErrServiceAuthFailed` shape the other 6 verifiers use — behaviorally equivalent; (b) a *concurrent* lead-conversion double-submit that races past the pre-check returns generic 400 `VALIDATION_FAILED` on the unique-index violation instead of 200-with-existing-row — no duplicate is created, error mapping only; (c) `account/internal/handler/duplicate_check.go:43` persists the duplicate-warning token and appends the `DuplicateWarningRaised` outbox row in two separate transactions — error is propagated (not swallowed), not a P0/P1 mutation. |

## Trend

Severity is monotonically converging across rounds: R1 = 3 BLOCKER; R5 = 2 BLOCKER + 7 MAJOR;
**R6 = 0 BLOCKER, 0 over-reach, 0 downgrade, 1 MAJOR + 3 MINOR + 1 LOW-batch.** The registered
11 are genuinely closed. This is a draining tail, not a recurrence.

## Decision

**REWORK #6 (consolidated micro-rework).** Per release-owner decision, drain the residual tail
in one final package before G12 passes, rather than passing with backlog. Findings registered as
BLK-G12-026..030 in `planning/blockers.md`; package `delivery/G12-rework-6.md` (same two hard
constraints: no downgrade AND no over-reach). After Codex remediates, Claude runs a final spot
re-audit of exactly these five items + a build/test confirm; if clean, **G12 PASSES**.
